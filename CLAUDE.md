# sshbox — how Claude sessions work here

Several Claude sessions work this repo at the same time. These rules keep them
from colliding on the user's tablet, and make sure nothing counts as done until
the user has tried it.

## Who runs the app

- One **coordinator** session holds the only `flutter run` / `flutter attach` on
  the user's tablet: a Xiaomi Pad 8 on wireless adb over the tailnet,
  `100.101.228.69:<port>`. The port changes whenever wireless debugging restarts.
  The coordinator is currently **sshbox development center**.
- Every other session and sub-agent is a worker. Workers never run `flutter run`,
  `flutter install` or `adb install`, and never launch or drive the app on any
  device or emulator. `flutter analyze` and `flutter test` are fine.
- The coordinator hands feature work to sub-agents, each in its own worktree. It
  answers the user itself, merges, reloads the tablet and keeps the UAT table
  below.

## Finishing a change

1. Work on a branch in a worktree. Merge `main` into it, and get
   `flutter analyze` and `flutter test` clean. Version numbers are CI's
   (`tag.yml`), not a commit's: nothing here bumps `pubspec.yaml`'s
   `version:` line, which decides only X.Y — a new major or minor is set
   there by hand. If merging `main` conflicts on it, take `main`'s. The
   repository has no git hooks, and `core.hooksPath` stays unset.
2. Fast-forward `main` to the branch and push. A push that changes the app
   releases it, as the user chose: `tag.yml` tags the next version and
   releases to Play's closed testing, the desktop builds to R2 and the notes
   to the website. The coordinator does this step for
   sub-agents, because git against the main checkout is blocked from their
   worktrees. If `main` moved in the meantime, merge it into the branch again and
   re-verify. `main` must always build.
3. Tell the coordinator the commit, what to test on the tablet, and what the
   change needs:
   - a hot reload, for most changes;
   - a hot restart, for new fields, `initState` or globals;
   - a full rebuild, for new plugins or native code.
4. The coordinator reloads the tablet (`kill -USR1 <pid>` for a hot reload,
   `kill -USR2 <pid>` for a hot restart) and asks the user to test. It then
   updates the table below.
5. A full rebuild never goes through `flutter run`'s own install. When that
   install fails, flutter uninstalls the app and installs again, and the
   uninstall wipes the app's data on the tablet (hosts, keys, settings). This
   happened once, when HyperOS rejected an install with
   `INSTALL_FAILED_USER_RESTRICTED`. Instead:
   - build with `flutter build apk --debug`;
   - install with `adb install -r -t <apk>`, which fails without uninstalling;
   - launch the app and `flutter attach`.
   If HyperOS blocks the install, ask the user to tap Install on the tablet.

## User acceptance test (UAT)

A feature is **not done when it is merged**. It is done when the user has tried
it on the tablet and said it is OK.

- **New feature:** as soon as work starts, add a row to the table below naming
  the responsible session.
- **Status:** update the row as the feature moves along:
  `in development` → `on tablet, UAT pending` → `UAT passed` or
  `UAT failed: <what the user saw>`.
- **Follow-up:** after every reload, the coordinator tells the user what to test.
  At the end of each report it lists every feature that is still
  `UAT pending` or `UAT failed`, and follows up with the user that its UAT has
  not passed yet ("UAT belum pass").
- **Failed UAT:** the feature goes back to its responsible session, or to a new
  sub-agent, with the user's exact words. The row stays open until the user
  confirms the fix.
- **Passing:** only the user's OK moves a row to `UAT passed`. Merged, tests
  green, or "should work" don't count.
- **Name the build:** a row that passes says which build it passed in, as
  `UAT passed in 1.0.69+73 ("their words")`. The user tests builds, not
  commits, so a commit hash alone is no use later: when a feature breaks, the
  build it last worked in is the only thing that gives a range to bisect, and
  only the coordinator hears the user say it passed, so only the coordinator
  can record the real one.
  The seventy older rows that recorded no build were stamped in one go with
  the build current at the time, on the user's instruction to treat everything
  already tested as passing now ("maka kamu nomorin ke build sekarang dengan
  anggapan semua yang sudah di test user adalah sudah pass"). Their wording has
  to keep saying that the stamp is the newest build the row is known to work
  in and **not** the first: a reader who takes it for the first will bisect
  into history where the feature already worked, which is worse than no number
  at all, because a wrong number is trusted. The 27 rows that already named a
  real build were left alone — a real record beats a stamped one.

## Features

Newest first. "Coordinator" means **sshbox development center**, working
through its sub-agents.

| Feature | Responsible session | Commits | UAT |
|---|---|---|---|
| Telemetry that counts who runs the app, catches what breaks, and turns a bug into a GitHub issue from inside the app: the counts go to a Cloudflare Worker of our own and the crashes to Sentry, the split the user chose | telemetry and issues | f08057b, 56cd31d | on main, waiting for the tablet, and one privacy hole closed before it could ship: the coordinator's builds had Sentry's native backend at none only because tools/build_desktop.sh exported SENTRY_NATIVE_BACKEND, while CI's release.yml calls flutter directly and never runs that script, so every CI desktop build would have got sentry_flutter's default, crashpad. crashpad sends a minidump — a raw copy of the process's memory at the crash, which in an SSH client can hold a private key or a password in use — and a minidump never passes through the scrubber, which only ever sees Dart events. Nothing leaked yet, CI having no DSN so Sentry never started, but the day a DSN was added it would have. none is now set in windows/CMakeLists.txt and linux/CMakeLists.txt, the one place every build goes through, CI's included, deliberately with no override; the script's two settings, dead once CMake decides and whose comment invited trying crashpad, are gone. Proven with nothing but CMake deciding, SENTRY_NATIVE_BACKEND unset exactly as on CI: the Windows zip holds sentry.dll and no crashpad_handler.exe or crashpad_wer.dll, and the Linux bundle holds libsentry.so and nothing of crashpad. One check along the way was wrong and caught: the zip first inspected was picked by name, and 1.0.65+65 sorts after 1.0.62+66, so it was a stale zip from before Sentry — the missing sentry.dll gave it away. Then, as before: where it needs a full rebuild (a new plugin with native code on every platform), and in the next release. **The Windows build is proven**, by the coordinator, the one machine here that can run it: Jeansh.exe built and packed into its 21 MB zip, exit 0. It took two tries and neither failure was Sentry's. The first found no Flutter on the Windows side only because FLUTTER_WINDOWS was unset — C:\src\flutter\bin\flutter.bat was there all along. The second failed in the jni plugin, dozens of MSVC C2059s in dartjni.c and jni.h: sentry_flutter 9.30.0, the newest stable, pins jni to 0.14.2 exactly, and 0.14.2 will not compile under MSVC while the 1.0.3 the tree had before would. An override upwards was shut by that pin, so the sub-agent vendored jni into third_party/jni with windows taken out of its platforms, the firebase_core pattern exactly — jni is Java interop and Sentry needs it on Android alone, there being no JVM for it to reach on Windows. Linux and Android were left untouched and both still build; the coordinator's Windows build is what proved the rest of Sentry links under MSVC, which until then was only reasoned. The coordinator also probed the scrubber itself with realistic Jeansh strings rather than taking the tests on trust — a tailnet hostname, user@ip, all three database URIs with their passwords, a Windows path holding the user's name, the Play service-account path, a PEM block, a GitHub token, an LC_SSHBOX_KEY value and an IPv6 literal with an embedded v4 — and every one came back redacted, the version number deliberately surviving. It found one rough edge, a host fingerprint half eaten by the path rule, which the sub-agent fixed with a lookbehind while keeping = out of it on purpose, since with = in it HOME=/home/trias would stop being a path and be too short for the key rule, and the login would leak; a test holds that. analyze clean, 848 passed, 1 skipped on the merged tree. Still unbuilt: macOS and iOS, there being no Mac here. Before that: on a branch, waiting for the coordinator's Windows build and then the tablet, where it needs a full rebuild (a new plugin with native code on every platform): sentry_flutter is 9.30.0 rather than the 8.14.2 pub picks by default, because 8.14.2 does not build on Android at all — its Gradle asks the Kotlin compiler for language version 1.6, which Flutter 3.47's no longer supports, and the sub-agent hit that before writing any feature code. The Windows fear turns out not to apply: sentry_flutter does not ship a prebuilt library there the way Firebase does, its sentry-native.cmake clones sentry-native and compiles it with whatever toolchain the build has, so there is no STL to mismatch — but the desktop build now wants a network at CMake configure time, and the crash backend, which would otherwise build crashpad through MSVC, is pinned to none in tools/build_desktop.sh (Dart errors still reported, a native desktop crash not). The coordinator then ran the Windows build, which failed — not on Sentry, whose reasoning held, but on `jni`: dozens of MSVC `error C2059` and `C2091` in jni\src\dartjni.c and jni\third_party\jni.h. sentry_flutter 9.30.0 pins `jni: 0.14.2` exactly, and that version's C does not compile under MSVC where 1.0.3, which the tree had before, does — so overriding upwards is shut too. jni is Java interop and there is no JVM for it to reach on Windows, so it is vendored at third_party/jni with `windows` taken out of its pubspec's `platforms:` and wired through dependency_overrides, exactly as third_party/firebase_core was for the same kind of reason; `linux` and `android` are untouched. Flutter now lists no jni at all for Windows and the vendored path for Android and Linux. Proven here: a clean Android debug APK builds and carries libdartjni.so and Sentry's own native libraries with JniPlugin still registered, and the Linux release builds and bundles both libdartjni.so and libsentry.so. Windows is still unbuilt here and the coordinator's `tools/build_desktop.sh windows` is the only thing that settles it; macOS is unbuilt too, and CI's macOS build settles the pod. The scrubbing is an allowlist rather than a strip list: the event is rebuilt field by field, so the user, the request, the server name, the breadcrumbs, `extra`, `debugMeta`, the threads, the device's own name, the OS `rawDescription` and every frame's source line, surrounding lines and local variables are gone by construction and a future SDK attaching something new cannot leak it; what is left goes through one scrubber for URIs, `user@host`, POSIX and Windows paths, IPv4 and IPv6, PEM key blocks, tokens and dotted hostnames, and the message of every exception whose text is host output or a user's path by construction (Jeansh's own, dartssh2's, dart:io's, PlatformException) is dropped whole with only the type kept. `print` breadcrumbs, failed-request capture, user-interaction breadcrumbs, performance tracing and session tracking are all turned off; a screenshot never was on. 33 tests, one of them serialising a whole event and asserting no hostname, login, password, IP or device name survives anywhere in the JSON — it fails if the contexts or the exceptions are passed through unrebuilt. One thing the coordinator's own probing found and this fixes: a host fingerprint read as `SHA256:47DEQpj8HBSa+<path>`, the path rule having started at a slash inside the base64 — nothing leaked, a fingerprint being public, but it read as though the scrubber had lost its grip. The path rule now refuses a slash that follows a letter, a digit or a `+`, so the run falls through to the key rule and goes whole as `<redacted>`; `=` is deliberately left out of that guard, or `HOME=/home/trias` would stop being a path and be too short for the key rule to catch, which a test holds it to. The Worker is written but not deployed: worker/jeansh-telemetry, its own rather than a second job on jeansh-notify, D1 rather than Analytics Engine because the anonymous relay needs a counter it can read back, with a README naming every one-off the user has to run and what the anonymous route does not protect against. analyze clean, 848 passed, 1 skipped. Before that: in development, with a sub-agent: not Firebase, since push_messaging returns early on every desktop and Windows has Firebase stripped out of third_party/firebase_core altogether, so Crashlytics would have covered Android alone. The counts are a random install id made once and kept locally — never a device id — sent at most once a day with the version, the build, the platform and the OS version and nothing else, through the same once-a-day SharedPreferences pattern the auto updater already uses, failing silently so a dead endpoint costs the user nothing. Sentry's DSN is baked with --dart-define as JEANSH_UPDATE_HOST is, and no DSN means crash reporting is off and says so rather than half-working. The load-bearing part is the scrubbing: this is an SSH client, so a stack trace, a breadcrumb or an exception message can carry hostnames, addresses, usernames, key paths, file paths, command text, tmux session names and database URIs with passwords in them, and none of it may leave the device — beforeSend and beforeBreadcrumb strip it, sendDefaultPii off, no IP attached, and tests feed the real exception types the app throws (DbException, FileBrowserException, GitException, UpdateException, dartssh2's own) and assert what is left. An ordinary refusal — a wrong password, a connection refused, a file that is not there — is not a fault and is not reported, or the free tier pays for noise. Report a bug offers both routes, the user picking each time, as they asked ("buat untuk keduanya jadi user bisa pilih untuk report sebagai anonymous atau bernama"): named opens a prefilled issues/new in their own browser through url_launcher, with no token anywhere and the issue under their own account, and anonymous posts to the Worker, which holds the token and opens it as a bot — the token living only there because this repo is public and anything baked in is readable with strings, the same reasoning as the updater's split host and path; the anonymous route is a public write surface, so it is rate limited per install id and per IP with a size cap, and what that does not protect against is to be said plainly rather than glossed. Telemetry is on by default, the user having chosen opt-out, so Settings gets a switch that turns both halves off — off meaning nothing sent at all and Sentry not merely muted — with a one-time notice on first run saying data flows and where the switch is, and Report a bug stays available with telemetry off, a deliberate act being different from background collection. Asked of the sub-agent before any feature code: whether sentry_flutter builds on Windows at all, since it ships native C++ there and Firebase's prebuilt SDK would not link against MSVC 14.40, which is the whole reason desktop has no Firebase — if it cannot, the fallback is the Worker taking crashes too, which is a different shape of work and must not be discovered at the end (asked for in "aku butuh untuk tambahkan telementry yang befokus untuk mengetahui jumlah user yang menjalankan applikasi dan menangkap bug atau error dan ketika ada bug mereka temukan bisa langsung buat issue di github", with "dan ada opsi untuk mematikan telementry di settings") |
| A diff opens in a file tab rather than in the Git panel's own small view: the panel keeps the list of what changed, and tapping one hands the diff to the file tab, which already has the terminal's font, find, copy and a remembered scroll position | git tab or drawer | 2b0e9aa | on main, waiting for the tablet, where it needs a hot restart (a new value on the TabKind enum and new fields on the session): tapping a change in Changes or a commit in History hands the diff to a file tab beside the shell, and the panel keeps its lists and stays where it was. The tab is the file tab, read-only, reached through a new GitDiffBrowser that is a FileBrowser serving one thing — the git command — so the diff goes through the same load, the same errors and the same Reload a real file does, and there is no second viewer to keep working; the virtual name ends in .diff, so the added and removed lines are coloured by the editor's own diff highlighter, which the project already had, and find, Copy content, Go to line, word wrap, text size and the scroll position come free with it. The tab holds the command rather than its answer, so Reload from host re-runs git, which is how a diff is refreshed after staging; the command is bound to the repository rather than to the panel, so a diff keeps working once the panel is closed or another repository is picked; a file both staged and unstaged gives two tabs, being two diffs. The three questions put to the sub-agent came back answered: the panel's own diff view is gone entirely, about 65 lines, and nothing is lost, it having had no find, no copy and no scroll memory; a diff tab is deliberately not restored after a restart, a saved tab being something to find again while a diff is the output of a command against a repository that will have moved on, and a test asserts the file tab beside it is saved while the diff is not; and the strip reads <host> · main.dart · diff, the chat tab's shape, with the host cut first, · staged diff for the staged one and abc1234 · diff for a commit. From the drawer, opening a diff shuts the drawer, the tab being underneath it. One fault the sub-agent found and proved by removing its own guard: git's refusal escaped the tab's error handling and left it spinning for ever, so GitDiffBrowser.readText now turns a GitException and anything else into a FileBrowserException — the same lesson the auto updater's review had just produced, applied before it could bite. 9 new tests driven end to end through the tab shell, and four lines dropped from the drawer test that asserted the route which no longer exists; analyze clean, 813 passed, 1 skipped, re-run by the coordinator, which also read the browser's write paths, all of which throw, and checked no NUL bytes and pubspec.yaml untouched. Before that: queued behind the Git panel's tab-or-drawer setting, both being the same files: read-only, with the added and removed lines marked, opened from Changes and from a commit in History; what happens to the panel's own diff view, and whether a diff tab is restored with the others after a restart, are to be settled by the sub-agent and reported (asked for in "sama git diff ini previewnya coba di perbaikin lagi, re-use tab file untuk tampilkan diffnya") |
| The Git panel opens where the user wants it: a setting makes the key bar's git button open it as a tab beside the shell, as it does now, or as a drawer over the terminal, the panel itself unchanged — the same repository picker, Changes, History and diffs, only somewhere else | git tab or drawer | 09b1ae8 | on main, waiting for the tablet, where it needs a hot restart (a new global read in main() and a new field on the terminal page), and in the release after v1.0.66: Settings gains a Git section with Open the git panel as, offering Tab or Drawer, Tab the default, saved as sshbox.git.drawer and read at the tap, so changing it needs nothing reopened and a tab already open stays a tab. In Drawer the button fills the terminal page's own end drawer, the one the files tree uses, at the same width, with a Close in the panel's header since there is no tab close there; the files button clears it, so each button swaps the drawer's content. The panel itself is untouched, as the user asked ("contentnya tetap sama"). Two faults the drawer turned up, both latent in the tab: the header's branch name sat in an unflexed Text, so at 349 dp on a 411 dp phone a long branch pushed Refresh and Close 199 px off screen and Close could not be tapped — now Flexible with an ellipsis; and the panel's reconnect rediscovery asked the session whether the tab was open, false in a drawer, so a reconnect would never re-find the repositories there — it now asks whether the repositories were disposed, the question it was really asking, and a session being disposed lets its repositories go, drawer mode never closing them. 5 new tests, and one line in settings_page_test's helper, the new section having pushed a row under the fold of the 800x600 test window. The coordinator merged it with the auto updater, which touches the same Settings page, and re-ran both together: analyze clean, 804 passed, 1 skipped. Known and reported rather than assumed: across openings the drawer keeps nothing, a Scaffold dropping its child when it closes, so the status, history, branch and scroll position are read again and a half-typed commit message is lost — hoisting the panel's state out of the widget is the surgery the brief ruled out; and with Drawer picked, a tab opened earlier stays a tab and can be on screen beside the drawer, both reading the same repositories. Before that: in development, with a sub-agent: Tab stays the default, so nobody's habit changes without asking; in Drawer the git button opens the terminal page's own end drawer, the one the files tree already uses, so the two share it and each button opens it with its own; changing the setting takes effect at the next tap and tears nothing open down, and a Git tab already open stays a tab. What the panel looks like at a phone's width is to be reported rather than assumed (asked for in "git tab ini di buat opsi di setting dong, bisa jadi tab atau bisa jadi drawer", with "contentnya tetap sama") |
| An auto updater for the desktop builds, the shape the user drew: the feed is a latest.json on the tag's GitHub Release — version, build, and for each of linux, windows and macos a path, a size and a sha256 — while the R2 host is baked into the build with --dart-define, so what the public feed carries is a path that says nothing by itself, and the app joins the two to download | desktop auto updater | 0237b7e, cc76577 | on main, desktop only and nothing for the tablet to see, needing a full rebuild and a served bucket before it does anything: the feed is the newest release's latest.json reached through GitHub's own releases/latest/download redirect, so one request, no token and no rate limit; JEANSH_UPDATE_HOST and this build's own JEANSH_VERSION are baked by tools/build_desktop.sh and tools/build_apple.sh, and a build missing either takes no updates and says so in Settings rather than half-working. Settings gains Updates → Check for updates, which always answers; the app also looks once a day at startup and says nothing unless there is something newer. A newer release offers its version and size, downloads with a bar and a Cancel that leaves nothing behind, is weighed against the size the feed gives while it comes down, and is kept only if its SHA-256 matches — a file that does not is deleted and named. Nothing is installed or run: it lands in Downloads with what to do with it per platform and an Open folder. A feed path that could leave the baked host — an absolute URL, a leading slash, a walk upwards, a doubled slash, a colon or a backslash — is refused unread, which is the whole point of splitting host from path: whoever writes the feed cannot choose where a build is downloaded from. The coordinator's review found two faults the sub-agent fixed in cc76577: Check for updates caught only UpdateException, so a TLS failure, the 60 s between-chunks timeout or a bad URL left the row spinning for ever with no way out but leaving Settings — the updater now turns everything it can throw into an UpdateException and both widgets catch the rest as a backstop, and a Downloads folder that cannot be written to says so instead of escaping raw; and Cancel tapped twice before the next chunk threw a StateError, now guarded. It also took two smaller notes: a backslash joined the rejected paths, and a 512 MB ceiling refuses a silly size before the update is offered rather than after the user consents. 21 tests in the file, none touching the network, the fetch injected; analyze clean, 804 passed, 1 skipped, re-run by the coordinator on the tree merged with the Git panel's tab-or-drawer setting, which touches the same Settings page. Two things stand between this and working: the user must serve the jeansh-builds bucket and set the repository variable JEANSH_UPDATE_HOST to it, and release.yml needs the feed job that writes latest.json from what is really in R2 and puts it on the tag's Release — that file belongs to the public repo session, so it goes over as a patch. That would be the project's first GitHub Release, which was deliberately avoided so nothing downloadable is left in public; it carries only file names, sizes and hashes, never a binary and never the host, and it is the shape the user drew. Signing the feed (an ed25519 key in CI, its public half baked beside the host) is the next piece, and installing in place — a helper that swaps the folder on Windows, a bundle swap on macOS, a directory on Linux — is a piece of its own, with notarisation the real blocker on the Mac. Before that: in development, with a sub-agent: desktop only, Android updating through Play; a check at startup at most once a day and a Check for updates in Settings, the download watched and cancellable, the sha256 checked before anything is offered and the file deleted when it does not match, and this first version hands the file over rather than replacing the app — Windows cannot overwrite a running exe, macOS wants a bundle swap, Linux is a folder, so that is a piece of its own. A build with no host baked takes no updates and says so rather than half-working. The coordinator told the user once, and they chose this shape anyway, that a host baked into a binary is readable with strings, so it hides the address from a reader of the feed and not from someone set on downloading; a token per user is the road to that, and signing the feed (an ed25519 key in CI, its public key baked) is the next piece either way. release.yml belongs to the public repo session, so the step that publishes latest.json comes back as a patch for the coordinator to hand over, and the user must serve the bucket and give CI the host once (asked for in "for desktop add auto updater", shaped by "metadata untuk auto updater di simpan di release github.com tapi untuk download link itu nanti akan di bagi jadi 2 R2 host yang akan di bake langsung ke app lewat env ketika build dan path buildnya yang di baca lewat meta data") |
| Pasting an image works on the Mac too: with a picture on the clipboard the macOS build said "Nothing on the clipboard a terminal can paste", because clipboardImage() returns null on anything but Android and only Android has the native half, so the paste fell through to text and found none; macOS gets its own, reading NSPasteboard for a picture or a copied image file and handing back a path the upload types at the prompt, as Android's does | Coordinator | 4760fb3 | on main, waiting for a Mac: the sub-agent was cut off by the account's session limit before its worktree even existed, so the coordinator wrote it. MainFlutterWindow answers the same channel and method MainActivity does, so the Dart side keeps one path: a picture copied in Finder arrives as a file URL and is taken name and all, and everything else as bytes, read PNG first, then JPEG, then TIFF, which macOS synthesises for almost anything and which is the largest; the copy goes to a directory of the app's own, emptied each time, 20 MB refused with a file weighed before it is read, and a pasteboard that offers a type then hands over nothing is an error saying what to do rather than silence. Linux and Windows are unchanged and say so. Tests: Cmd+V on macOS puts the picture on the host and types its path, which fails without the change, and where no native half answers a paste is text; analyze clean, 761 passed, 1 skipped. The Swift is unbuilt, there being no Mac here: the CI macOS build or the Mac session settles it. Before that: in development, with a sub-agent: the same channel method and the same shape, the copy taken natively so no bytes cross the channel, one image at a time, 20 MB refused before anything is copied, named pasted-YYYYMMDD-HHMMSS.png where the clipboard gives no name, and a picture that cannot be taken saying which app refused rather than falling silent. Linux and Windows are left as they are. The sub-agent has no Mac, so the Swift is unbuilt until the Mac session or a CI macOS build tries it (a bug the user hit: "build macos ketika paste dari clipboard berisi gambar dapat notif Nothing on clipboard a terminal can paste, padahal aku lagi hold gambar yang kalau aku paste ke applikasi lain jadinya gambar") |
| The SQL view asks by field as well as by hand, the way TablePlus's Filters bar does: a table opened from the side list gets a Filters tab beside the free SQL one — a row per condition with its column, an operator (=, ≠, >, <, ≥, ≤, contains, starts with, ends with, is null, in), a value and a tick to leave it out without deleting it, AND or OR between them, + and − to add and drop rows, Apply to run and Clear to empty | mongo query tabs | 3593256 | on main, waiting for the tablet, where a hot reload is enough for a database tab opened after it (an open one keeps its old page until it is closed and reopened), and in the release after v1.0.65: a PostgreSQL table opens on Filters — a tick, a column from that result's own columns, an operator (=, ≠, >, <, ≥, ≤, contains, starts with, ends with, in, is null, is not null), a value where one is wanted, AND or OR once there are two rows, + and −, Clear, and Run relabelled Apply — beside the SQL tab, which is the old box unchanged. Tapping a table runs the same SELECT it always did. A row not filled in is named before anything is sent. The value goes in as an escape string, E'…', doubling quote and backslash, which reads the same whatever standard_conforming_strings is set to, and a NUL is now refused rather than sent, since a simple query ends at one — that quoting was the grid's own, lifted to pgLiteral so saves and filters share it. contains, starts with and ends with escape the value's own %, _ and backslash and carry an ESCAPE, so "contains 50%" means 50 per cent. A filtered result is still one table's rows, so the grid still edits and saves. Proven live against PostgreSQL 17.11 in the sub-agent's own container, since removed: a value holding a quote, a backslash, a per cent, an underscore, a newline and non-ASCII comes back as itself, on a server with standard_conforming_strings off as well, contains 50% does not match 50 off, and a save through the filtered result reads back; removing the LIKE escaping fails that test, so it is load-bearing. 16 unit tests and the widget tests in the MongoDB tabs' style; analyze clean, 778 passed, 1 skipped, re-run by the coordinator, which also read the quoting and the SQL the conditions build. Known ceiling: an item in an in list cannot hold a comma. Before that: queued behind the MongoDB tabs, both being the same page: the value goes into the SQL as a literal, since the PostgreSQL client here speaks simple queries only and binds nothing, so quoting is the thing to get right (a value with a quote, a backslash or non-ASCII, proven against a real server) or the client learns to bind; the result still comes from one table with its key among the columns, so the grid stays editable as it is today (asked for in "lalu untuk ui sql aku butuh kamu buatkan ui untuk bisa free query dan bisa juga query berdasarkan field yang ada seperti ini", with a TablePlus screenshot) |
| MongoDB's query split the way Mongo Compass splits it, and aggregation at last: the one free-text JSON box becomes tabs — Find, with Filter and, behind Options, Project, Sort, Limit and Skip; Aggregate, a pipeline built stage by stage, which had no way in before; and Command, today's free-text box kept for what the tabs do not cover, such as listIndexes or distinct | Coordinator | 410d79b | on main, waiting for the tablet, where it needs a hot restart (the page gained fields), and in the release after v1.0.63: the editor shows Find, Aggregate and Command for MongoDB and nothing new for PostgreSQL or Redis. Find has Filter, with Project, Sort, Limit and Skip behind Options, and a collection tapped in the list fills it and runs it at once; Aggregate is a stage a field, in order, each removable, with Add stage offering $match, $group, $sort, $project, $limit, $lookup, $unwind and $count as templates; Command is the old box, for what neither covers. A field that is not JSON, or a limit that is not a number, says which field before anything is sent. An aggregate's rows are read-only, a lock beside the count saying why, since a pipeline's rows belong to no one collection; a find's stay editable. Changing tab asks about unsaved changes, as Run does. The sub-agent that started it was cut off by the account's session limit with nothing committed; the coordinator took its work over, finished the tabs, and wrote the tests: 11 of its own, analyze clean, 759 passed, 1 skipped. It also found the three live MongoDB tests failing on a container the same sub-agent had left holding port 57017, which the live tests expect for themselves, and removed it. Before that: in development, with a sub-agent: a Find result stays editable as now, an aggregate result is read-only, since its rows map back to no one collection, and says so rather than failing at save; unsaved grid edits are asked about when tabs change, as Run already asks; PostgreSQL and Redis keep the single editor. Proof asked for: the command each tab builds, a live aggregate and find against a real MongoDB in a throwaway database, and the fake-session widget tests (asked for in "aku perlu kamu develop untuk mongodb querynya di pecah menjadi seperti punya mongo compass, dan ini belum support aggregate query aku butuh kamu buatkan 2 tab jadi user bisa pilih tab query find dan tab query aggregate") |
| A record of every tmux pane, kept by the host: tmux itself pipes each pane of the app's sessions to a file under the user's home, so what `clear`, a redraw or the scrollback cap takes from the pane, and what happened while the phone was locked, is still there to read from the tab | pane record | 6404567, fff8958 | on main, waiting for the tablet, where it needs at least a hot restart (a relaunch wants an APK), and in the release after v1.0.62: at every attach every pane of sshbox-<id> gets pipe-pane -o, and after-split-window and after-new-window hooks on that session alone give panes made later theirs with nothing attached — none set with -g, and none where the user has a global hook of that name, which a session one would hide; records go to ~/.local/state/jeansh/records/<session>/<server pid>-%<pane>, made under umask 077 through directories none of which may be a link and only ever created new (set -C), turned over at 8 MB into .1 by ulimit -f, 16 MB a pane (32 where sh counts that in KB), and pruned at attach when untouched for 7 days with their pane gone. "Keep a record of each pane" sits under Use tmux, on by default, older hosts too. A tmux tab's long press offers Pane record: the focused pane's record read over SFTP from its last 512 KB, Load earlier, Refresh, replayed into a terminal of the pane's size that keeps every line scrolled off, where a clear scrolls rather than wipes and a full-screen program's screen is kept at each clear and at exit. Claude Code draws only what changes on its own full screen, so its record is frames at best; chat mode's transcript is its record. The sub-agent's live tests prove a cleared line kept, output with nothing attached recorded, a pane split with nothing attached recorded, 0600/0700, nothing on the user's other session and no global hook, and each fails with its part broken; 748 passed, 1 skipped. The coordinator read the host scripts — the prune removes only regular files under records, find not following links, and empty folders — and found one fault in the tests: the live tests ran with the real HOME while the attach prunes records against the test server's panes, which would have taken this machine's own records for gone (there were none yet), so they now run under their own. Measuring Claude Code, the sub-agent ran claude in the repo, which left a transcript titled "sshbox development center" holding two test prompts, ~/.claude/projects/-home-triasbrata-dev-sshbox/9e20ab48-643d-4724-80a2-fd4617cef96d.jsonl, for the user to delete or keep. pubspec.yaml's line goes to 1.0.62+66 with it. Before that: in development, with a sub-agent: pipe-pane for every pane of the app's own sessions and hooks on those sessions alone, so recording goes on with no client attached; 0700/0600 under ~/.local/state/jeansh/records, capped and pruned; a per-host setting on by default for tmux hosts, since a record cannot be switched on after the fact; read over the session's connection, tail first. Chat mode already is the record when Claude Code is the agent; this covers every other program (a user's feedback, passed on by the user with "harusnya ini sebagian sudah ada tinggal tambahkan yang belum ada": "tmux is the right answer for surviving a dropped connection. The part that bites later is that a pane is not a record: scrollback is capped, and anything that clears or redraws the screen takes the history with it. So on reopen you get the current state, not what happened while the phone was locked. Worth capturing the session separately when an agent is the one driving it.") |
| Chat reaches a whole transcript, and says an interactive session is read-only: it shows the last 512 KB of a session's transcript, less than an agent can write in an hour with the phone locked, so earlier turns load on demand; and a Claude Code session somebody is typing into at a terminal, which chat follows live but will not type into, is announced as if what is sent went into it | chat full history | 6703745 | on main, waiting for the tablet, where it needs at least a hot restart (new fields on ClaudeChat, and the page's list became a CustomScrollView), and in CI's first real release: "Load earlier turns" at the top of a conversation reads 2 MB of the transcript at a time through earlierCommand, the same finder and once-per-shell quoting as the history with only numbers added, every chunk ending where a line starts so no event is read in halves, one bigger than a chunk left out with a notice, a tool result read before its call kept until the call is reached, and at most about 32 MB of one transcript read into a chat; earlier turns grow upwards from where the chat opened and live ones downwards, so neither moves what is on screen. A session open at a terminal is announced as live and read-only, its box disabled with "Read-only: type into “name” at its terminal". The sub-agent measured 41–65 ms a chunk on this machine's 112.9 MB transcript, and paging back matched a one-shot read of the last 10 MB exactly; 735 passed, 1 skipped. The coordinator read the diff — the command carries only numbers past the finder, a chunk's last byte is the newline ending the line before what is shown, so an event bigger than a chunk is caught and skipped until its start — and re-ran it. A reconnect re-picks the session and drops the earlier turns loaded. Before that: in development, with a sub-agent, from the same feedback as the pane record: paging back through the transcript in chunks through the once-per-shell quoting, whole events across a chunk's edge, the scroll position kept as older turns go in above; the interactive session's notice and input row saying typing happens in its terminal |
| CI numbers and releases every push that changes the app, and writes its notes onto the website, the flow the user drew (developer push → main → CI numbers it and tags it → the tag builds → desktop to R2 → release notes on the website): tag.yml, on a push to main touching lib/, android/, ios/, macos/, linux/, windows/, assets/, third_party/ or pubspec.*, takes the highest v* tag ("Jeansh X.Y.Z, build N") and tags the pushed commit vX.Y.(Z+1), build N+1 — X.Y.0 when pubspec.yaml's line asks for a new X.Y, the one thing that line still decides — with the run's own GITHUB_TOKEN, as the user asked ("ini bukannya bisa pakai token github action aja langsung ya tinggal di grant aja aksesnya"): GitHub refuses Actions as a ruleset bypass actor on a personal repository ("must be part of the ruleset source or owner organization"), so the v* tag ruleset now keeps only moving and deleting a tag admin-only, and since a GITHUB_TOKEN tag starts no workflow, the same run calls release.yml for it as a reusable workflow; the `release` environment now lets only main deploy, so its secrets reach a run on main and nothing else, and a v* tag made anywhere else releases nothing; nothing is committed back, so no version line conflicts; release.yml's `version` job reads the number off the tag and every build writes it into its own pubspec.yaml (committed in the runner only, as tool/release.sh wants a clean tree); `notes`, after android and desktop succeed, sends the app's commit messages since the previous tag to anthropic/claude-sonnet-5 through OpenRouter's OpenAI-compatible API (tool/release_notes.py; the repository variable RELEASE_NOTES_MODEL overrides it) for a title and up to six bullets in English and Indonesian, put first in site/releases.json in the R2 bucket, which jeansh-web's new /releases page (jeansh-web e58cb71, deployed) reads through an R2 binding at request time, so no deploy per release and no token for jeansh-web; a notes list R2 fails to read stops the job rather than being overwritten; the local version hooks are the coordinator's to retire | public repo ci | | in development: OPENROUTER_API_KEY set from the user's key and checked against OpenRouter; no GitHub token needed any more (asked for in "harusnya tag otomatis terbuat dan ini pakai claude access token aja nanti aku buatkan access token untuk di pakai claudenya", the drawn flow in "harusnya flownya seperti ini", its choices — every push that changes the app, CI alone numbering, the model writing the notes — and "aku kasikan token dari openrouter aja jadi di call pakai openapi compatible api") |
| A tmux tab keeps what a pane writes in its first moments: on attaching, after a reconnect and for a pane split off, the tab reads the pane's history from tmux and starts the terminal over from it, and output tmux sent in the same read right after answering was drawn first and then wiped, so a command typed just after attaching could show nothing until something redrew, though tmux had it | Coordinator | | on main, waiting for the tablet, where it needs at least a hot restart, a pane gaining a field: found by the public repo session's new CI, whose tmux_live_test "the device's variables reach new panes" timed out on GitHub's ubuntu-latest and was read as tmux 3.4 not passing LC_SSHBOX_KEY on. The coordinator ruled that out in an ubuntu:24.04 container, tmux 3.4 giving the first pane key-1 and a split after a reattach key-2, as 3.2a does, and then made it fail there 8 times in 15 with every core busy — the tab's pane showing only the prompt while tmux's own capture held the command and <key-1 host-1>. While a pane's history is read its output is now held, each piece counted against the answers tmux had given by then, and what came after the capture's answer is drawn on top of it; 0 failures in 15 under the same load. A unit test whose fake tmux answers a capture and goes on in one read draws '' where the output was without the change |
| Desktop builds kept in R2 and nowhere a user could download them, mobile straight to its store: on a v* tag release.yml's `android` job runs tool/release.sh --publish to Play's closed testing as before, with nothing sent to R2, and its `desktop` jobs build Linux through tools/build_desktop.sh, Windows on GitHub's Windows runner into the same zip that script makes through WSL, and macOS through tools/build_apple.sh, signed ad-hoc as the project already is, keeping each with its SHA256SUMS in the private R2 bucket jeansh-builds under desktop/<target>/, the latest build only — each release syncs with --delete and the one before goes, as the user asked ("untuk upload ke r2 nga perlu di buatkan release code, langsung aja di replace data sebelumnya karna kita nga butuh simpan artefak build sebelumnya"), and the old per-version folders were moved or deleted by hand; the bucket has its r2.dev URL off and no custom domain, the run keeps no Actions artifact and makes no GitHub release, and its public log names neither the bucket nor an object, both being secrets of the release environment with aws s3 cp printing nothing; the R2 key itself is the user's to make, scoped to that one bucket, since wrangler's login cannot make API tokens; the App Store and a notarised macOS build wait on an Apple Developer account | public repo ci | 6d32fdd, 1637773 | UAT pending: run 35458658677, a dry run, built 1.0.6+65 for Linux, Windows and macOS on its second attempt, and each is in the bucket, checked by fetching its SHA256SUMS back, while GitHub holds no artifact and no release and no job log names the bucket, the account or R2's host; the user made the R2 token and the session set R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY from it (asked for in "lalu semua buildnya di arahkan untuk di store di r2 aku tapi jangan sampai ada meninggalkan jejak yang bisa di download oleh user", then "yang ke r2 hanya build desktop untuk mobile ke playstore langsung atau appstore") |
| A licence like Aseprite's: LICENSE makes Jeansh source-available rather than open source — the official builds on Google Play are paid for, anyone may build it from this repository and use their own build free of charge for any purpose on their own devices, and nobody may hand the source or a build on to anyone else, free or paid, a GitHub fork excepted; a contribution may ship in the paid builds, the name and icon are not licensed, and the fonts, Mermaid and third_party/ keep their own licences. Glyph, the other example, is AGPL-3.0 instead, which lets anyone pass builds on or sell them, so the stricter one was taken: it can be loosened to AGPL later, while code once released under AGPL stays there; every dependency is BSD, MIT, Apache or MPL-2.0 (dbus, gtk), none of them in the way | public repo ci | ed4b224 | UAT pending: waiting for the user to read LICENSE (asked for in "aku mau buat licencynya di buat seperti glpyh(https://glyphformac.com) atau aseprite ( https://glyphformac.com/ ) dimana user hanya bayar untuk versi buildnya tapi kalau user build sendiri dari github maka dia boleh untuk pakai for free") |
| Versioning: the build number N still goes up on every commit that changes the app, but the version name is semver rather than a copy of it — the patch goes up once every ten builds, counted from the build its tag was made at, a major or minor is set by hand with the patch back to 0, and each new name gets an annotated git tag vX.Y.Z on the first commit that carries it, so a new major or minor and every patch are tagged; the Windows, Linux and macOS folders and third_party/ now count as the app too | Coordinator | e8587ed, f93da27 | the version names go on from 1.0.62, at the user's word ("naikkan versi tag ke latest release build itu di 1.0.62 setelahnya baru ikutin aturan baru"): v1.0.62, annotated "Jeansh 1.0.62, build 66", tags a1d6840, the commit CI's first real release put on Play's alpha track as build 66 under the name 1.0.7, so tag.yml's highest tag by version is v1.0.62 and the next app push is v1.0.63, build 67 — checked by running tag.yml's own next step here; pushing the tag released nothing, release.yml starting only from tag.yml or by hand. pubspec.yaml's line, still 1.0.6 and only what a local build shows, goes to 1.0.62 with the next app change rather than making a release of its own. Before that: retired at the user's word ("ok", asked whether to retire the local hooks and take CI's numbering alone), the user having chosen in the public repo session that CI numbers every app push and releases it (e3aeeab, 69bd0ec): .githooks, tool/test_hooks.sh and core.hooksPath are gone, pubspec.yaml's line now decides only X.Y, and README, CLAUDE.md, release.sh, play_publish.py and PLAY_CONSOLE.md say so; the local v1.0.6 is the very tag object on GitHub, so CI's numbering starts from it. Before that: on main, UAT pending: the user answered in the public repo session that the tags should go up by themselves ("harusnya tag otomatis terbuat dan ini pakai claude access token aja nanti aku buatkan access token untuk di pakai claudenya"), which tag.yml (3f8b281) does on GitHub with CLAUDE_GITHUB_TOKEN, and the hooks and docs say so; the coordinator found that tag.yml's search for the first commit carrying a name reaches back into the 1.0.N+N history, which used 1.0.13 to 1.0.65, so from 1.0.13, about build 135, it would tag and release a days-old commit, and handed that back to the public repo session with a fix; still open, whether the name stays 1.0.6 or starts at 1.1.0. Before that: waiting for the user, who had to say whether the tags should go up by themselves: tool/test_hooks.sh passes and fails on the old hooks, flutter analyze clean and 730 passed, 1 skipped, checked by the coordinator, and the first commit on the new hooks tagged itself v1.0.6. `.githooks/pre-commit` takes the patch from X.Y's newest tag and raises it when N is ten past the build in that tag's pubspec.yaml, so a boundary crossed without the hook is caught up on the next app commit, and an old 1.0.N+N line a merge from an older branch brings back is named again from the tags rather than trusted; an X.Y nobody has tagged yet is kept as written, so a hand-set 1.1.0 or 2.0.0 is never overridden. `.githooks/post-commit` makes the tag, only for a first X.Y or the patch one past the newest, warning about a name that skips ahead instead of tagging it, and still brings the index up to the commit after `git commit <paths>`, now the whole X.Y.Z+N; `tool/release.sh --name X.Y` sets X.Y.0. The tags stay on this machine, push.followTags off: the public repo row's release.yml publishes to Play's closed testing on every pushed v* tag, landing on main minutes after this was committed, and pushing the hooks' tags with main would have released to testers every ten builds without anyone asking, so whether they should is put to the user and a tag goes up only by hand until then. v1.0.6 had already gone up with the first push, which main refused as behind while the tag went through; it tags e8587ed, a commit from before release.yml with no workflow in it, and the coordinator checked that no release ran. The name moves from 1.0.65 to 1.0.6, the 65 builds of 1.0 at a patch every ten, tagged v1.0.6 at build 65, so 1.0.7 comes at build 75; the build number, what Play and Android compare, is unchanged. core.hooksPath pointed at the main checkout's .githooks, a working tree left at 3118e00, so no change to the hooks merged into main would ever have run; it is now the relative .githooks the README always gave, each worktree running the hooks its own branch holds. tool/test_hooks.sh drives all of it in a throwaway repo and fails on the old hooks (asked for in "aku butuh kamu untuk perbaiki mekanisme versioning kita, aku mau setiap kali commit itu emang nambah build number tapi tagging version major.minor.patch itu pakai yang seharusnya jadi itu bakal perlu perbaikan di git commit hook, dan setiap commit akan buat tag baru jika, - major/minor berubah, dan patch naik setiap 10 itteration maka itu akan ada tag baru") |
| Public repository, CI and the guards around it: triasbrata/sshbox goes public, after gitleaks found no real secret in any commit on any branch (its hits are the test's fake keys, the README's placeholder key, Firebase's client key, which every APK carries anyway, and minified Mermaid), and the upload key's password turned up in no commit; being public, Actions costs nothing, so the pipeline 02f2fec took out to save money comes back: ci.yml runs flutter analyze and flutter test as the job `check` on every pull request and push to main, and release.yml runs tool/release.sh --publish on a v* tag (a --dry-run when started by hand), with the upload key and the Play key as secrets of a `release` environment only v* tags and main can reach; pull requests and issues can only be opened by collaborators, main cannot be deleted or force-pushed and asks anyone but an admin for a reviewed pull request with a green `check` (the owner's direct pushes still go through, so step 2 above is unchanged), only an admin can make a v* tag, a fork's workflow waits for approval, GITHUB_TOKEN is read-only, and secret scanning with push protection, Dependabot alerts and private vulnerability reporting are on | public repo ci | 6cb2878, ca50cb6 | UAT pending: public, the three rulesets, collaborators-only pull requests and issues and the release environment all read back from GitHub; a dry run of the Android release built, signed with the upload key and reached Play without committing; CI's one failing test was an app race the coordinator fixed in cfbf01f (asked for in "buat jadi repository publik dan build pipeline script di pasang terus semua penjagaan untuk repository ini juga di pasang agar hanya contributor yang boleh contribute.") |
| Windows and Linux builds: the project had ios/ and macos/ but no linux/ or windows/, so both platforms are added, the desktop behaviour the macOS build has — no key bar keys or magic key, links to the machine's own browser, a Local shell card through flutter_pty — extended to them, every plugin with no implementation there gated so it never throws at startup, and a build script packaging a Linux .tar.gz and a Windows .zip | Coordinator | 0056431, ceafdd8 | built, both, waiting for the user to try them: ~/jeansh-builds/1.0.65+65/ holds Jeansh-1.0.65+65-linux-x64.tar.gz (19 MB) and Jeansh-1.0.65+65-windows-x64.zip (21 MB), both built from commit ceafdd8 and both matching their SHA256SUMS, checked by the coordinator; the zip holds Jeansh.exe with the patched flutter_pty.dll and no Firebase file at all. The Windows road there: Flutter 3.47.2 for Windows put in C:\src\flutter by the user; then ATL, which the user added to Build Tools 2022; then the link failed in firebase_app.lib, Firebase's prebuilt C++ SDK needing STL helpers newer than MSVC 14.40 — __std_remove_8 and two more — and the user chose to drop Firebase from the desktop rather than update the toolchain ("yaudah di matikan saja notifikasi untuk desktop yang lewat fcm"), since FCM has no desktop implementation and a desktop gets its notifications over the SSH connection. firebase_core 4.14.0 is copied to third_party/ with windows taken out of its platforms, through dependency_overrides; Android and macOS register Firebase exactly as before and the APK still builds. Before that: Windows: Flutter 3.47.2 for Windows was installed by the user at C:\src\flutter, the same version as here, and the build ran as far as MSBuild, which stopped at `atlstr.h` and `atlbase.h`: flutter_secure_storage_windows and flutter_local_notifications_windows need ATL, and Build Tools 2022 has no atlmfc folder for any MSVC version, so the sub-agent's word that the C++ toolchain was complete was wrong. The ATL component is the user's to add. The script also needed the Windows folders on PATH, which this shell did not have although interop is on. Before that: Linux built and run, waiting for the user; Windows waiting on Flutter for Windows. Linux: sudo asked no password, so clang, libgtk-3-dev, liblzma-dev, libstdc++-12-dev and libsecret-1-dev went on by apt; the release bundle is ~/jeansh-builds/1.0.63+63/Jeansh-1.0.63+63-linux-x64.tar.gz, 19.5 MB, its SHA256SUMS checked by the coordinator; launched headlessly under its own Xvfb, D-Bus and HOME, it drew Home with the Local shell card and opened a bash login shell that ran a command — the coordinator looked at the screenshot — and a second launch hands over to the first, Linux now single-instance like the others. Running it found what compiling never showed: flutter_local_notifications throws UnimplementedError for launch details on Linux, which silently killed the transfer notifications, now skipped with a test that fails without it. Also gated: Linux and Windows notification settings, without which it throws at start; FCM on every desktop; and mermaid, shown as source with a copy button where webview_flutter has nothing. Windows: the coordinator's guess was wrong — Build Tools 2022 with MSVC 14.40, the CMake component, SDK 10.0.22621.0 and Developer Mode are all there; Flutter for Windows is the one thing missing, so tools/build_desktop.sh windows runs to the call of flutter and stops, and nothing was installed on the Windows side. WSL, Windows alone: a card per distro beside Local shell, docker-desktop and rancher-desktop left out as Windows Terminal leaves them, wsl.exe by its full path with -d <name> --cd ~, each its own argument, the list decoded as UTF-16LE and tested on bytes captured here. flutter_pty 0.4.2 wrote the program into its Windows command line twice and quoted nothing, so wsl.exe -d Ubuntu would have run its own path as a Linux command (upstream #19, #22 unmerged); it is vendored at third_party/flutter_pty with its LICENSE and only src/flutter_pty_win.c changed — the coordinator diffed it against the published 0.4.2 and read the quoting, the standard CommandLineToArgvW algorithm — and MSVC here opened Ubuntu through the patched ConPTY code, printing /home/triasbrata. The Android APK still builds from the vendored copy, checked by the coordinator. 729 passed, 1 skipped. Linux needs a Secret Service for saved passwords and has no .desktop entry; Windows is unsigned, so SmartScreen warns, and every shell starts about a second late. Before that: in development, with a sub-agent. Linux builds on this WSL once clang and the GTK 3 headers, both missing, are installed — by apt if sudo asks no password, otherwise by one command the user runs. Windows cannot be cross-compiled: it needs Flutter for Windows and Visual Studio's C++ workload on the Windows side, neither found in the usual places, and nothing is to be installed there without the user's word. On Windows alone, the Local shell also opens WSL — wsl.exe by its full path under the ConPTY flutter_pty already uses, starting in the Linux home, the distros read from wsl.exe -l -q, which writes UTF-16LE rather than UTF-8, and the option absent where WSL has no distro (asked for in "build untuk windows dan linux" and "di windows buil pastikan untuk bisa execute wsl, dan ini hanya ada di build windows") |
| Tab groups: two tabs, or more, shown as one tab, each page in a pane as tmux draws them: a long press on any tab — shell, file, web, chat, Git, database or Transfers — offers Group with…, which asks which tab or group it goes beside and shows it there, focused; a group sits on the strip as one outlined tab holding its tabs' own chips behind a group button, which goes back to the pane last focused and whose long press offers Stacked (or Side by side) and Ungroup; the panes sit side by side or stacked, with a grip between each two that drags to share the room out (no pane under 48 dp), and the focused pane is outlined in the theme's accent as tmux outlines its own; only the focused pane takes keys, so none goes to a pane not outlined: a touch on a pane, or a tap on its chip, moves them there, the chip without raising the soft keyboard; each tab's ✕ and long press still work inside the group, Take out of group puts one back on its own, and a tab that closes leaves its group, a group of one being none; groups last until the app closes, not across a restart, since tabs get new ids each run | tabbed pane grouping | 94926a9 | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed), on the user's word ("oke restart"). The coordinator re-ran it on the merged tree, analyze clean and 662 passed, 1 skipped, and read the focus handling: the pane not outlined sits under ExcludeFocus and each pane has a FocusScope of its own, so the terminal's parked-focus rescue can act only in the outlined pane and two visible shells cannot fight over focus. Build 60 by a version-only commit, main and the branch having each reached 59. Groups do not survive an app restart yet, tab ids being made new each run. Before that: in development (asked for in "aku butuh kamu untuk build feature untuk bisa 2 tab di gabung jadi 1 group tab dan ketika jadi 1 group tab setiap tab berubah menjadi pane seperti tmux pane") |
| Git panel: the key bar's new git button opens a Git tab beside the shell — a repository picker holding the folder's own repository and the ones checked out inside it, the way Zed lists them; Changes with stage, unstage and Stage All and a commit box under it; History; and every diff drawn in the terminal's own font. It runs over the session, so a saved host's repositories come over SSH and a local shell's through a process beside it (`CommandCapable` on `LocalTransport`) | apple build pipeline | f9c25f2, 9598fa9, 3b9d793 | reopened: on any repository with commits, History errored instead of listing them, and Changes was seen doing the same — found by a promo session taking screenshots on build 1.0.62 against a demo repository with five commits and two uncommitted changes, with git 2.34 and 2.54. Every command ran as git … 2>&1 then printf of a status marker, read from a line starting with it; git log --pretty=format: separates its entries rather than ending them, so the last commit had no newline, the marker was glued onto it and never found, and the whole log came back as an error. The marker now starts with a newline of its own at the one place every command goes through, and a test runs the repo's own command through a real sh with a git that answers as git log does, failing on the old marker with the very line the promo session saw. Fixed in 3b9d793, build 1.0.64+64, on main and waiting for the tablet. The earlier pass was most likely on a repository with no commits, where log prints nothing. The promo holds the Git panel until the user has tried it again ("Perbaiki dulu, baru promosi"). Before that: UAT passed ("sudah pass ini untuk sesi awal"), after: the tab threw "A GitRepos was used after being disposed" every time it was closed, and a host with no file tree root found no repository at all, because the login home was quoted as a path and the host was sent `cd '$HOME'` ("gitnya masi ngebug"). The bar's buttons were also thumb-sized on a desktop ("size iconnya kegedean untuk skala desktop"), so the bar there is 34 tall with 15pt icons. Asked for in ("keybar kalau android jadi bottom bar seperti di vscode jadi ada icon icon kecil yang bisa di klik untuk … github (new feature otomatis ikut root repo project, tapi kalau dalam root project punya child folder yang punya repo user juga bisa pilih seperti punya zed)") |
| The desktop build loses what stands in for a missing keyboard and gains the one thing a phone cannot have: no key bar and no magic key, every link to the machine's own browser rather than a web tab (the Tailscale sign-in too), and a Local shell card on Home that opens a real pty through `flutter_pty` running the login shell. The macOS app sandbox goes off with it, without which that shell would see the app's container instead of the user's home | apple build pipeline | ee9a657, 9c08c5a, 0e4ece2, a59d24f | UAT passed ("sudah pass ini untuk sesi awal"), after: connecting on the Mac works now ("berhasil connect ke wsl"), after it could not read the host's saved password — "PlatformException(Unexpected security result code, Code: -34018, Message: A required entitlement isn't present.)" ("ini kenapa?"). The sandbox going off took the implicit keychain access group with it, and flutter_secure_storage_darwin 0.4.0 asks the keychain for synchronizable items, which needs that entitlement; the lock now takes 0.4.3, whose `skipSynchronizableQueries` does not. Adding `keychain-access-groups` was tried and put back: Xcode signs it only with a development certificate, and this app signs ad-hoc. The bar then came back without its keys (0e4ece2): taking the whole key bar away had taken the files, upload, git and Claude buttons with it, and there was no way left to reach them ("tinggal bottom barnya perlu di balikin") — that part is what is still to be tried. Asked for in ("magic keyboad, magic fab  need to disable dan add local exec bash  on desktop build", "tab browser juga di disable untuk desktop version") |
| The chat comes back where it was left: opened again, a chat returns to the position it was scrolled to rather than the top, and one that was at the bottom stays at the bottom as a live session goes on | Coordinator | 7fd972b | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): positions are kept in memory by host and session for as long as the app runs, so switching tabs, picking another session and coming back, and closing and reopening the tab all return to the place; a session left at the bottom comes back at the bottom and keeps following. Not across an app restart, a restored tab not reopening a session. Before that: in development, with the chat sub-agent (asked for in "di chat last position chatnya di ingat jadi kalau user buka kembali chatnya user bisa kembali ke posisi chatnya") |
| The database browser's filter is its own for each kind of database: an SQL view for PostgreSQL, a NoSQL view for MongoDB and a KV view for Redis, each shaped the way that database is organised — schemas and kinds of object, databases and their collections, key patterns and key types — over the in-memory side list that just passed, with Redis past its 2000-key cap still asking the server | Coordinator | ce78b8a, db2c585 | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed). SQL: tables grouped under their schema, which folds and counts; the filter takes schema.table; a chip per kind actually present — tables, views, materialized views, foreign and temporary tables — materialized views listed for the first time, from pg_matviews checked with has_table_privilege, since information_schema has none. NoSQL: databases and their collections as a folding tree; the filter takes database.collection, keeping a match's database in view; chips for collections, views and time series, the type coming free with listCollections. KV: text holding *, ?, [ or \ is a Redis pattern matched as SCAN MATCH does, by a port of Redis's own matcher that a live test holds to a real server over 14 patterns; each key shows its TYPE, fetched in one pipelined batch per load rather than one call a key, and chips narrow by it; past the cap the pattern and the type both go to the server as SCAN … MATCH … TYPE, which wants Redis 6. One change to what just passed, flagged to the user: plain text in the KV filter is now case-sensitive, as Redis keys are, so the in-memory and the server's answers always agree. One filter function and one pane serve all three. Redis's transaction is now built on the new pipeline, the grid editing that passed riding on it, and its tests are green. The coordinator re-ran it on the merged tree, analyze clean and 671 passed, 1 skipped, and checked that a pattern and a type go to Redis as arguments of their own, that the materialized-view query quotes its identifiers on the server, and that opening one goes through the identifier quoting tables already use. The query editor and the result grid are unchanged, read as out of scope (asked for in "untuk db ini aku butuh untuk interface filternya di buat unique untuk setiap jenis database, jadi kalau pakai redis beda, mongodb beda, dan pg beda. kalau aku bakal pecah jadi 3 jenis tampilan untuk nosql view, sql view, dan kv view") |
| A chat tab reads `<hostname> · Claude`, as a file tab reads `<hostname> · file`, the host giving way first when the strip is short of room | Coordinator | 4c3a47c | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): it read Claude alone, so two chats on two hosts could not be told apart; it takes the host the same way a file tab does and the same cut, and a test that looks for the tab's close tooltip failed on the old label and passes on this one — the cut itself is the file tab's test's, the test font being too wide to measure it on this label (asked for in "tab chat di title nya di buat seperti tab file, jadi nama host divider Claude") |
| The chat sidebar keeps its sessions when it is hidden and shown again | Coordinator | 7fd972b | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): the list is held by the page, so hiding and showing the panel or closing and opening the drawer keeps it without asking the host again; ↻ and a new chat refresh it. The tablet's own cause was never reproduced — in the fake host the list came back, only with a second request — and the fix removes that request either way. Before that: in development, with the chat sub-agent; the likely cause is the list living in the panel's own state, thrown away when the panel is hidden and never asked for again (a bug the user hit: "ketika user click collapse sidebar session on this host, terus expand lagi sessionnya jadi empty") |
| Chat mode's tool rows open to what they hold, and its sidebar says what now happens: a collapsed tool row in a watched session expanded to nothing, and the sidebar still told the user a running session is picked up as a copy that nothing said reaches — the fork's wording, the opposite of what has happened since typing went into the session itself | Coordinator | 22caed8, 7fd972b | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): the sidebar's heading now says a running session is watched live and what is sent goes into it, and a finished one is continued where it stopped; no other fork-era wording was found. The tool rows are 22caed8, below. Before that: the tool rows on the tablet, UAT pending: the fix is in build 1.0.59+59 and was hot-reloaded onto the debug build on the tablet on 2026-09-19. The guess the coordinator first handed over was wrong: a debug build attached to the tablet showed the real cause at once, fourteen times — type 'bool' is not a subtype of type 'double?', thrown restoring a scroll offset. The ExpansionTile keeps its open-or-shut bool in page storage under PageStorageKey(run.id), and the two scroll views inside the row, the input and the result, had no key of their own and so stored their offset under the same identifier, reading the bool back as a double; a release build draws a widget that threw as nothing. Each block now has its own slot, and a widget test that opens a row throws the tablet's own error without it; after the reload the error stopped. The sidebar's stale wording is still with the chat sub-agent. Before that: in development, with the chat sub-agent: the transcript's tool results are the standard shape, a string in a tool_result block, and present — 3 of 4 in the last 400 KB, the fourth still running — but that session's transcript is 110.5 MB with only about 40 events in its last 400 KB, so a tool's call and its result easily straddle the history and the live follow; the leading guess is a row marked done at the end of the history and never filled in when its result arrives through the follow (a bug the user hit: "bagian colapse ketika di expand isinya masi kosong", with a screenshot) |
| The chat button checks the host's Claude Code version first: tapped where the host's Claude is older than the oldest version with everything chat mode relies on, it shows a toast naming the host's version and the minimum instead of opening a chat that could only fail | Coordinator | 7fd972b, 7f2085a | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): the minimum is 2.1.259, which added --permission-prompts none, the newest flag chat uses — agents --json came in 2.1.145, waitingFor in 2.1.162, id, state and --all in 2.1.169, --bg by 2.1.140, pinning in 2.1.147, attach before 2.1.198, by the changelog. Read from claude --version through the same finder and quoting, the last line only, compared part by part as numbers; too old, a toast names both versions; an answer that is no version keeps chat shut and says what the host said. Checked once a connection, only a pass remembered. Before that: in development, with the same sub-agent as the New chat button and the finished sessions, since all three touch the same files; the minimum is to be found from when each feature arrived — stream-json, claude agents, attach, --bg, pins.json — not set to whatever this machine happens to run (asked for in "tinggal memastikan claude version sebelum chat ini eligble, jadi hotbarnya ketika di klik tidak meet dengan minimal versionnya maka akan balikin toast kalau versionnya nga meet dengan minimal versionnya") |
| Typing into a running session works on Claude Code 2.1.277: from that release a bracketed paste is recorded as <pasted_content>, so the chat never recognised its own message, never showed it, and Claude answered "Your message only has pasted text in it… Should I follow it?" instead of doing it — found by the chat sub-agent, 2.1.277 having reached the host at 01:09 and four of the user's six sessions having restarted onto it at 01:11 | Coordinator | 7fd972b | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): up to 600 characters goes in as keystrokes, measured taken as typing where 1,868 were taken for a paste; a newline is Ctrl+J, which the input takes as a line break; a tab becomes spaces, since Tab takes a suggestion; and a leading ! gets a space, because typed bare it switched the input to bash mode and ran the rest on the host, measured even in plan mode. Everything typed first goes through the same filter as a paste, every control but tab and newline removed, so no Ctrl+C, escape or carriage return reaches the input as a key — checked by the coordinator. Longer is still a paste, its tags stripped when the transcript is read back, and Claude may ask about it. A session blocked but idle with no waitingFor has ended on a question and is typed into now; one at a permission prompt shows waitingFor and is refused, naming what it waits for. Known and left: a message beginning / runs a slash command and one beginning # may be taken as a memory, as typed at a terminal. The user's earlier pass was most likely on a 2.1.276 session |
| Chat mode gets a button to start a new chat | Coordinator | 7fd972b | UAT passed on build 1.0.62+62's code, hot-restarted onto the debug build on the tablet on 2026-09-19 ("new chat pass"): a chat tab now starts empty and starts nothing; its first message runs claude --bg with the ⋮ mode in the host's file-tree root, and the tab then watches that session and types into it like any running one, so every chat is listed, pinnable and survives the app closing and none is lost from the list. New chat sits in the sidebar's head and the ⋮, and leaves the session it was on running. Measured: claude --bg with a prompt returns in about 0.8 s and is named after it; a follow-up through attach is recorded 1.4–1.5 s after the tap locally, a second of it the app's own waits, and slower than a -p chat's stdin. The cost: a tool needing approval stops the session at a prompt the app cannot answer, and the chat points to claude attach. The first message goes on the host's command line, so on a host shared with other users it shows in ps while claude --bg starts. An open question for the user: new chats take the ⋮ mode, Accept edits by default, rather than the host's own default mode. Before that: in development, with a finding the coordinator measured first: a chat started from Jeansh is a claude -p process, which claude agents --json lists only while it lives, as kind interactive with no id, and drops the moment it ends — so a new chat that simply ended the old process would lose that conversation from Jeansh without a word. The shape handed over is to start each new chat as a background session, followed and typed into through what the running-session work already built, so it is listed, pinnable and survives the app closing; whether a background session can start with no prompt, and what 1–3 s a message through attach costs a fresh chat, are to be measured first, and the fallback, a plain fresh claude -p, has to say the old conversation will no longer be listed (asked for in "tambahkan button untuk mulai baru chat di chatmode") |
| Chat mode follows a running session live: picked up while its process is alive, a session is continued as a copy, and a copy never hears what the original goes on to say, so the only way to see a background session's newer turns was to tap it again in the sidebar, which reads the original's transcript afresh; the user expects the chat to be a live view of that session, new replies arriving on screen as the session writes them | chat mode feature | c3b66ed | UAT passed on build 1.0.57+57, installed on 2026-09-19 with its data kept ("mantap sudah bisa disend dan sycn dengan yang terminal"). One tail -f from exactly the byte the history read stopped at, ended three ways that leave nothing on the host — the session's pid going, the channel closing, or the tail dying — with no tail left behind proved through a real shell. What is typed goes into the original session through claude attach on a PTY the SSH exec channel now takes, a new capability carried across the transport isolate as pure additions, the shell, SFTP and tunnels untouched and the isolate transport tests and transfer bench green: the listing is read afresh before every message and only done or working is typed into; the text is a bracketed paste with every C0 control but tab and newline, DEL and every C1 control removed, so an ESC[201~ in it cannot end the paste; one attach at a time per chat; and a message shows Sending… until the session's own transcript records it, or Queued when the session queued it behind a running turn, and says so if neither happens within 30 s. Proved end to end on this machine against a real background session: "What is 7 times 8?" drawn back as 56, live, 3 s after sending, and the session still running afterwards. The fork code is gone, nothing using it now. The coordinator re-ran it on the merged tree — which also carried the Apple pipeline and git panel another session had pushed to main — analyze clean and 658 passed, 1 skipped, and read the transport diff and the paste sanitiser for itself. Before that: in development, handed to the chat mode session: the transcript on the host is appended a line per event, so following it as it grows is the likely way, and what sending a message into a session that is still running does is settled: the user chose to type into the original session from the chat ("aku ikut saranmu untuk sessi ini"), having been told it costs 3–5 s a send and a day's work. Measured by the chat mode session on throwaway background sessions: the CLI has no way to send into a running session but claude attach, and a message bracketed-pasted through claude attach landed as a real turn in the original session, idle or mid-turn, and was answered in order, with nothing forked or stopped; a session sitting on a dialog was never produced, so only the states measured safe, done and working, are typed into and every other one — blocked, or a name a later CLI invents — is refused with a word to open it in a terminal; a message shows as sending until the session's own transcript records it, and says so if it does not. It needs a PTY on the exec channel, plumbed through the transport's isolate (asked for in "sama ini nga realtime chatnya, user harus re-click ke sidebar history untuk menampilkan latest chatnya, harusnya jika background tambah response baru di layar chatnya juga otomatis ke update") |
| Chat mode shows a session as pinned when it is pinned in claude agents | chat mode feature | c3b66ed, 7fd972b | on tablet, UAT pending: in build 1.0.62+62's code, hot-restarted onto the attached debug build on 2026-09-19 (the APK underneath is still 1.0.58+58, so a relaunch of the app loses it until an APK is installed): the sidebar lists claude agents --json --all in three sections, Pinned in pin order, then Running and Finished (N), each newest first by startedAt, lazily and uncapped — 81 rows, 21 KB, 0.23 s here; a finished one is continued in place. Before that: UAT failed on build 1.0.57+57, installed on 2026-09-19 with its data kept: only pinned sessions seemed to show ("selain yang di pin juga di tampilkan bukan hanya yang sudah di pin saja yang muncul"). The coordinator measured why: the sidebar lists running sessions, and on that host all six running background sessions are pinned, none running unpinned — so what the user misses are the finished ones, 74 done and 5 stopped under claude agents --json --all, left out on purpose when the user had asked for the running ones; a finished session can be continued in place, the CLI allowing a resume once it is not live. Back in development with a sub-agent, the chat mode session having ended. Read from jobs/pins.json in the same round trip as the listing, pinned sessions first in the file's order with a pin in the sidebar, a missing or unparseable file meaning nothing pinned; whether the agent view orders pins the same way is not verified, the file's order being the natural reading. Before that: in development, unblocked: the pins live in ${CLAUDE_CONFIG_DIR:-~/.claude}/jobs/pins.json, a JSON array of the short job ids that claude agents --json gives each background entry as id — found by snapshotting every file's modification time, having the user pin a session and diffing, pins.json going from 72 to 86 bytes, after claude agents --json (no pin field in 73 entries), the jobs' state.json, the daemon's roster.json and ~/.claude.json had all come up empty, since the file sits directly in jobs/ and never says "pinned"; handed to the chat mode session to read beside the listing (asked for in "sekarang aku butuh chatnya juga ikut di pin jika di claude agents terpin", and "kalau tidak bisa ya sudah dulu" while it looked impossible) |
| After a paste or a share, Claude Code's input in the terminal goes unresponsive for under three seconds: whether that is Jeansh — its UI thread held, or input dropped while focus or the keyboard connection comes back after the app is brought to the front by a share — or Claude Code on the host busy with what was pasted, is not yet known, and the two want different fixes | Coordinator | | in development: being measured before anything is changed, with the tablet's log captured while the user repeats a paste and a share, and the user asked whether letters typed during the stall appear afterwards, which means input arrived and something was busy, or are lost, which means Jeansh dropped them (a bug the user hit: "setelah paste/share somehow input field claude jadi unresponsif beberapa detik (dibawah < 3detik)") |
| Database browser: the side list's filter answers at once: the tables, collections or keys are read once, when the tab connects, and each letter typed narrows that list in memory instead of asking the database again after 400 ms behind a spinner, as it did (a PostgreSQL information_schema query, and for MongoDB a listDatabases and one listCollections per database, all through the SSH tunnel, on every pause in typing); the ↻ Refresh in the filter and Reconnect read it anew, so a table, collection or key made since shows only after one of them; a Redis list that stopped at its 2000 keys still asks the server when filtered, as before, since the keys past them were never read | cache search collection performance | 606ce67, 8b5cb19 | UAT passed ("filter daftar db sudah pass"), first on the tablet in build 1.0.55+55; the coordinator re-ran it on the merged tree, analyze clean and 609 passed, 1 skipped, and checked that the filter narrows through the same _addGroup the database query used, so what matches is unchanged (asked for in "ui search collection table terasa sangat lambat ini bisa coba di cahce di memory aja supaya lebih cepat dan kalau user force refresh baru kita fetch dari db lagi") |
| Chat mode, the Claude Code plugin's panel in a tab: the key bar's new speech-bubble button, beside Browse files, opens a chat tab next to its shell, and what it talks to is Claude Code on the host itself, through `claude -p` with `--input-format stream-json --output-format stream-json` on one exec channel of the session's own SSH connection — the doorway the VS Code plugin uses, so nothing is scraped off a terminal: each event is drawn as it arrives, the user's words in a bubble, Claude's as Markdown through the renderer the file preview already has, and every tool as a row naming it and the one line it is about (the command, the file, the pattern) that opens to its whole input and what came back, spinning until its result arrives and red when that result is an error; it runs in the host's file-tree root so what Claude reads is what the drawer shows, one process serves every turn, and its session id is kept so restarting — after a reconnect, or to change what Claude may do without asking (Plan only, Accept edits, Allow everything, in the ⋮ beside the box) — resumes the same conversation rather than starting a new one; a prompt nobody can answer is never waited on (`--permission-prompts none`: a tool that would ask is refused and Claude says so in its reply, which the mode is there to avoid), an exec channel's shell is not a login shell so Claude is looked for on PATH, in ~/.local/bin, ~/.claude/local and the usual package managers, then in the login shell's own PATH, and a host with none says so instead of sitting silent — stderr is folded into stdout because the channel carries only stdout; the tab is saved and comes back like every other, carrying only that it was open and never a word of what was said, and it starts nothing until it is first shown | chat mode | dbf3fb8, 1224dae | UAT passed on build 1.0.51+51, installed on the tablet on 2026-09-18 with its data kept ("oke chat modenya berjalan working fine"). The coordinator fixed one fault on top before merging, in 1224dae: the command wrapped its script in sh -c '…' and spliced a quoted directory and session id into it, and a quote inside a quote closes the outer one rather than nesting, so both reached sh bare — a file-tree root with a space in it split and chat mode never started, and a $( ) in either ran; neither crosses a trust boundary, but a root with a space is ordinary. It got past the tests because three of them read the command's text, which read right and ran wrong; a test now runs it through a real shell with a stand-in claude over a directory with a space, a quote, a $( ) and a semicolon, and fails on the old quoting. 593 passed, 1 skipped after the fix. Before that, as the chat mode session left it: built — `flutter analyze` clean and 592 tests passing, 1 skipped, with the parser driven by a recorded transcript and the page driven end to end by a fake host. It needs a full rebuild rather than a reload, being a new tab kind with its own page and restore path. The wire format was not guessed: it was recorded off the `claude` on this machine, which is also how the permission question was settled — without an SDK host to answer a prompt, the CLI refuses the tool in-band and tells Claude so, rather than hanging, so v1 needs no control protocol and the ⋮ picker is what decides. Eleven hours of the elapsed time were spent waiting on the user to settle what the mode should be: Claude Code on the host, the Anthropic API from the tablet, or re-rendering the interactive terminal — they picked the first (asked for in "aku mau tambahkan mode baru yaitu mode chat seperti plugin claude code di vscode") |
| Sharing text to Jeansh pastes it into the terminal: a link shared from Chrome or a snippet from another app arrives as EXTRA_TEXT with no stream, and MainActivity read only EXTRA_STREAM, so it came through as an empty list and silently did nothing — while the share filter's */* still offered Jeansh for text. It goes in as a paste through the terminal's own paste path and never with an Enter, because a shared text is untrusted and a newline typed into a shell runs what came before it; it reaches the same session a shared file reaches, by the same route | Coordinator | c3fd5b4 | UAT passed on build 1.0.54+54, installed on the tablet on 2026-09-18 with its data kept ("share link sudah pass"). MainActivity now reads EXTRA_TEXT when a share carries no file, and the text rides the same queue as a shared file. Line breaks at either end are dropped, since there they could only be an Enter; the rest goes through xterm2's own paste, whose sanitiser replaces ESC and the other control bytes — so an ESC[201~ planted in the text cannot close the bracket early, which the coordinator checked in xterm2's source — and brackets it when the program asked. Where the program did not ask, a plain sh or an old bash that would run each line, text that is not one safe line is not pasted at all: it goes on the clipboard, with a toast saying so, for a paste the user makes knowingly. The page title Chrome sends as EXTRA_SUBJECT is never read, assumed rather than checked on a real Chrome; a caption with a file is dropped and the file uploads as before; with nothing open the text waits for the next host; past 64 KB it is refused. 599 passed, 1 skipped. asked for in "share ke jeansh kalau text harusnya jadi ke paste, ini casenya kalau user click share ke jeansh maka link yang di share langsung tertulis saja" |
| Chat mode picks up the Claude Code sessions already running on the host: `claude agents --json` lists every active session, interactive and background, with its id, session id, name, directory, kind, status and state, and needs no TTY, so it can be asked over the session's own SSH connection; `claude attach <id>` opens a background one live but is a terminal program, and `claude -p --resume` — how chat mode continues a conversation — starts a new process on the transcript, so what a still-running session does when resumed from the chat has to be settled on the real CLI before anything is built on it | chat mode feature | 579dd9a, a718d05, e2c8f22 | UAT passed on build 1.0.56+56 ("chat sudah pass"), with two follow-ups asked in the same breath, now rows of their own above: pins, and following a running session live. Fixed in e2c8f22: from 840 dp the sessions sit in a 300 dp panel on the left and stay, below it they slide in as a drawer the way the files drawer does, one button at the head of the input row hides and shows them, the bottom sheet is gone; and a session picked up now shows what it already said, read from its transcript on the host — found by its id under ${CLAUDE_CONFIG_DIR:-~/.claude}/projects rather than by a guessed path, an id not UUID-shaped never looked for since find -name reads a * as a pattern however it is quoted, the last 512 KB only (transcripts here run p99 79 MB, the largest 108 MB, one line alone 12 MB) with the tab saying when the earlier turns are left on the host — replayed through the same handlers a live turn goes through. A fork's transcript was measured to carry the earlier turns. The coordinator re-ran it on the merged tree, analyze clean and 615 passed, 1 skipped, checked that the history command is quoted once for each shell, and that nothing of a transcript is logged. Before that, it failed the sessions should be listed in the sidebar rather than a bottom sheet, and a session picked up shows none of the conversation it already had ("list sessionnya di buat di sidebarnya saja bukan bottom sheet, dan ketika resume chat percakapan di chat tersebut tidak muncul"); back with the chat mode session. The second is expected of how it was built: claude -p --resume in stream-json replays nothing on stdout, so the history has to be read from the transcript the host keeps, ~/.claude/projects/<the cwd, / and . turned to ->/<sessionId>.jsonl, one JSON event a line. Measured on the real CLI with a throwaway --bg session rather than any of the user's: `claude -p --resume` on a session whose process is still alive is refused outright — exit 1, "Session … is running as a background session … Add --fork-session to branch off a copy instead" — and identically whether it was busy or idle, so the line is alive or not, and two processes can never write one transcript; `--fork-session` on the same live session gives a new session id with the conversation carried across, the original running on untouched; after `claude stop`, a plain resume continues in place. An entry has a pid while its process is there and loses it once gone, and that is what decides: a session with a pid is picked up as a copy and the tab says so, one without is continued in place. Nothing ever runs `claude stop` — ending somebody's session from a tap is not that button's business. An interactive session is listed and never taken over, the CLI giving it no id for attach to address anyway. The list is everything on the host, not only under the file-tree root, each row showing its directory; finished sessions are not listed, the user asking for the running ones. The listing and the resume go through the same once-per-shell quoting, proved by running them through a real shell. Unmeasured: a fork of a live interactive session, since a throwaway one needs a TTY; if the CLI refuses it, the refusal shows in the tab. The coordinator checked that `claude stop` appears only in comments, and that a live session is forked every time and only once. 607 passed, 1 skipped (asked for in "selanjutnya aku butuh dari chat itu bisa integrate dengan "claude agents" jadi sessionnya yang jalan bisa kita dapat gitu") |
| The terminal's upload button takes several files at once: the key bar's upload was the one way in still picking a single file — the files drawer's Upload here… already picked several, and a share of several already arrived as several — so it now picks any number and sends them one at a time, in the order picked, through the same upload the share sheet uses; each types its /tmp path with a trailing space, so they land on one line ready for Claude Code or a command to take them all, and a file with no path on the phone, from a cloud provider, is passed over while the rest still go | Coordinator | ff5d138 | on tablet, UAT pending: in build 1.0.54+54, installed on 2026-09-18 with its data kept: one toast still says Uploaded for each file, which is honest but noisy for ten, and the button's tooltip still reads Upload a file to /tmp, both left as they are to keep the change to what was asked (asked for in "add support multi file upload") |
| Pasting an image, the three faults the log showed: a held Ctrl+V pastes once per auto-repeat, since the chord handler does not tell a repeat from a press, so holding the key would upload the same picture again and again; a clip from Chrome's file provider produces no file and says nothing at all, which is what made the whole feature look broken when it half worked, so a picture that cannot be read has to say so; and the file is named as a wall of digits, which works and is safe but is what the user will be typing into Claude Code | Coordinator | 7c326b2 | on tablet, UAT pending: in build 1.0.49+49, still there in 1.0.51+51, installed on 2026-09-18 with its data kept, with the coordinator's reading of the auto-repeat corrected in the doing: the repeats were never uploading. A repeat is a sibling of a key down in Flutter, not a kind of one, so the guard already dropped them — but dropping meant saying "not mine", which let each repeat fall through to xterm2's own paste shortcut, whose activator takes repeats by default. That is what the three clipboard reads in 100 ms were: text-only pastes, which did nothing for a picture and pasted a *text* clip four times for one press. Repeats are now claimed and dropped, so one press is one paste and nothing falls through behind the app's back. Chrome: the clip is now read from ClipDescription, which the clipboard service keeps itself — no round trip, no grant, and the question every other app asks — with the provider's own getType left as the fallback, since Chrome writes the picture lazily and answers null while it is still being made, which was read as "no image here". Silence is gone: a clip that says it holds a picture and then will not hand it over names the app that refused, and a paste that finds nothing usable says so. The name is pasted-20260918-045939.png where the provider gave none worth keeping, and a real name like a screenshot's is left alone. adb logcat -s JeanshPaste tells the whole story of one paste — what the clip declared, which item was taken, and the failing step with its authority and exception — and never the picture, never a URI path or query, which can carry a token. What is still only compiled and reasoned about is all of it: the Kotlin is driven by a mocked channel in the tests, and nothing about a real Chrome clip is proven until the tablet. The honest limit, worth knowing before the next round: ChatGPT gets its clip through OnReceiveContentListener, which Android applies a read grant to and which covers Ctrl+V, the IME and drag alike, because its compose box is an editable view — this app intercepts a raw key and reads the clipboard itself, which works for most sources but cannot be promised for every provider; the bulletproof answer is a real invisible editable behind the terminal, deliberately not built, since it would collide head-on with the IME-connection rules 57b0bd6 and aa5110f fought for. found by the coordinator reading the tablet's log against the file's timestamp after the user reported the paste failing and then working |
| The host key prompt names the machine that actually answered: with a second address, the key was pinned against the profile and the prompt showed the saved address, so a connection that came up over the alternative asked the user to trust a fingerprint under the name of a machine that had never been contacted — the user proved it by putting a wrong address in the main field and the right one in the alternative, and being asked to save a fingerprint for the main one | Coordinator | 8e222f7 | UAT passed on build 1.0.51+51 ("fingerprint pass"). The store was already keyed per address, `<address>:<port>`; e311277 simply handed it the saved address whatever answered, so "pinned against the profile" was never really implemented — it was per-address storage being fed a lie, and fixing it is the smaller diff, not the larger one: the dial now returns which address answered and that rides through to the check. The prompt names it — "Trust 192.168.1.20?", "First connection to my box at its alternative address, 192.168.1.20" — and the key is pinned there. The second prompt that per-address keying would normally cost is paid only when the two addresses show *different* keys: the same key already trusted at the host's other address is pinned silently, since only the machine holding that private key can sign the exchange, so it is the machine already trusted under another name. When they disagree the prompt carries the red shield and shows the other address's key beside it, saying that two addresses of one machine show the same key so this is either a different machine or something sitting on the address — and deliberately not the words "the host key has changed", which a test asserts are absent, so that warning keeps meaning an attack. Pins made before all this are untouched, the storage key and entry format being unchanged, and a host with no alternative address dials only its saved address, so nothing is orphaned and nothing is asked again. The squat case is better than it was: the intruder's key is pinned under the intruder's address alone, where before it was pinned as the machine's and the real host afterwards looked like an attack. a fault the user found while passing the alternative address itself ("5 pass tapi key finger print alternative address malah ketukar jadi aku tadi coba masukkan ip salah di utama dan ip benar di ke 2 lalu muncul save fingerprint untuk host utama padahal itu yang connect ke ip alternative") |
| The promo website explains how to send a notification: an FAQ a developer can follow end to end without asking anyone — that nothing has to be installed, which of the two ways applies when (direct over the SSH connection while it is up, the signed relay when the app is not connected at all), copy-pasteable examples building a title and body from variables, the signature scheme taken from the relay's own source rather than guessed, and what to do when nothing arrives | Coordinator | jeansh-web fe7b629 | live at https://jeansh.brata.cloud/notifications since 2026-09-18, published on the user's word ("faq di push saja auto deploy harusnya"); the push to jeansh-web's main did not deploy it — five minutes on, the live page was still the old one — because Cloudflare's GitHub integration was never connected, which only the user can do in the dashboard, so the coordinator deployed it with bun run deploy from that same commit and checked the new sections on the live page. Until the integration is connected, a push to that repo publishes nothing by itself. Its examples were run against the relay's own code with a throwaway key and FCM mocked; nothing went to the live relay or a real phone, and nobody has looked at it on a phone screen. Asked for in "di websitenya jeansh tidak ada faq untuk menjelaskan gimana caranya kirim notifikasi, dan aku butuh kasi contoh untuk integrasi dan kirim notifikasi dynamic ke device mereka"; it is built on a branch and deliberately not deployed, a push to that repo's main publishing the site through Cloudflare's GitHub integration, which is the user's call |
| Images linked from a Markdown file show as pictures in the preview rather than as their alt text: a source that resolves to a file on the host is fetched and drawn where it is written, through the same streaming download and 20 MB cap the image tab already uses, with its copy deleted when the tab closes; a web source stays refused and keeps the alt text, since a document off a server can carry a tracking pixel and fetching it would say the user is reading this file | Coordinator | beb52c1 | UAT passed on build 1.0.51+51 ("gambar di md ppreview pass"). A relative source resolves against the directory of the .md file, an absolute one is itself, a ~ goes through the login home the way every other path in the app does, and .. is walked and stops at /; a percent-escape is decoded, so a name with a space works. Nothing is confined beyond that, since the login can cat those files in the terminal anyway, and only a name isImageFile already knows is ever fetched — an .svg or a .heic is never downloaded and keeps its alt text, dart:ui being unable to draw either. One picture is capped at 20 MB, the image tab's own number now shared with it, refused on the size that lands with the first chunk so nothing big comes down, and a document at 64 MB of pictures in all, which bounds what one tab can leave in the cache; past either, the alt text says which. A spinner shows while it fetches, and each failure says what it was — no longer there, permission denied, disconnected, not a picture this app can draw, too large, or the document's budget spent — never a blank space or a broken box. The copies live in one directory per tab and go when the tab closes, with the fetches still running cancelled and whichever of the two finishes last removing the directory. The pictures are held by the tab rather than the preview, so a reload, a trip to Source and the lazy list all find a picture already in hand and paint it at its final height in the first frame, which is what keeps the restored scroll position from being pulled about; the claim that a first arrival does not jump the page is reasoned rather than measured, and the tablet is what settles it. asked for in "ketika render md file image yang di link ke md file tersebut tidak terbuka hanya seperti ini" with a screenshot of an article whose screenshot showed as an icon and its alt text |
| The terminal's content cannot be scrolled, and only killing brings it back: a state the app latches with no way out, which is what "kill it and it works again" looks like — the program's own mouse mode would clear when the program exits, and the alternate screen having no scrollback is not something a kill escapes | Coordinator | 0d2f720 | UAT passed for now on build 1.0.51+51 ("scroll terminal, input keybiard mati untuk sementara fix dulu"), the user holding both open to come back if either returns. The cause is one line: an armed CTRL called xterm2's setSuspendPointerInput, meant narrowly to hold the tap back so a Ctrl+tap opens a link rather than being eaten by a program reading the mouse — but that switch is blunt and takes tap, scroll, drag and move together. On the normal screen it never showed, xterm2 scrolling its own scrollback through a plain Scrollable that asks the controller nothing; on the alternate screen, which is where Claude Code, vim, less and tmux live, a drag is turned into wheel events or arrow keys for the program and that path does ask, so a suspended drag produced nothing whatever — and on a tablet there is no wheel to fall back on. CTRL now takes the tap alone and leaves the scroll, so whatever arms it and forgets to disarm it, scrolling survives, which is the fix rather than chasing an arming path nobody could prove; a scroll under Ctrl reaches the program as a wheel event, which is what a desktop terminal does, and before it reached nothing at all, so nothing is given up and every Ctrl+tap test still passes. Two candidates for what leaves CTRL armed: the key bar's own, which a second tap clears and so would not need a kill, and a hardware Ctrl whose key-up was lost, which is process-wide Flutter state and would need exactly the kill the user describes. Worth knowing: a stuck CTRL also means a tap on the terminal never raises the keyboard, because that path returns early — one stuck CTRL would explain the input going dead as well, and leaving the app and coming back is what would clear it. Unproven, but it is the first thing to look at if either comes back. a bug the user hit ("ini juga tiba tiba user tidak bisa scroll content terminal, harus di kill dulu baru bisa lagi") |
| Mermaid 12 draws the Markdown preview's diagrams: the bundled dist/mermaid.min.js goes from 11.17.2 to 12.0.0, which still sets the same global and still takes every option render.js sets, so the page loads it as before; 12 bundles the ELK layout engine and makes it the default, and draws in its new neo look, both taken as they come, which is why the asset is 5.6 MB rather than 3.6 and why diagrams will look different — a comment in render.js names the two lines that bring 11's drawing back; 12 wants ES2024 and the tablet's WebView is 152, far past it | Coordinator | d4cdb5e | UAT passed on build 1.0.54+54 ("mermaid 12 pass"), first on the tablet in build 1.0.45+45; the two mermaid tests fake the WebView and prove nothing about the drawing, so the tablet was what settled it, with docs/notification-architecture.md and ~/mermaid-gallery.md, one of every kind added since Mermaid 10 (asked for in "make sure render mermaid js support v12") |
| Pasting an image into a terminal uploads it and types its path, the way Claude Code takes a screenshot on a desktop: a terminal is a byte stream and an Android clipboard image is a content:// URI, so there is nothing to send down the wire, and Claude Code running on the host cannot see the tablet's clipboard either — the only thing it can be handed is a path to a file already on the host | Coordinator | 75cb1b9 | UAT passed on build 1.0.45+45 ("ah sekarang sudah bisa paste sebelumnya nga bisa"), at the second attempt and with three faults the coordinator found in the tablet log rather than on screen, now their own row above. It failed first ("paste gambar masi belum bisa padahal aku coba di app lain bisa seperti chatgpt"), and the log settled what the screen could not: the gesture was a hardware Ctrl+V and it reached the native read, which read the clipboard 15 ms after the key, so the wiring was never the problem; that clip came from Chrome and produced no file at all, while a second Ctrl+V 17 seconds later put a 630x487 PNG on the host in 75 ms, mode 0600, which the coordinator checked by hand. Nothing happened before and nothing could: every paste route read the clipboard as text and got null for a picture. Now a paste that holds one uploads it through the very same upload the paperclip and the share sheet use — exclusive create at 0600, replace by rename so a planted link cannot be followed, the Transfers tab, the notification and the bar along the bottom — and types /tmp/<name> with a trailing space. No quoting is needed because the name is scrubbed to letters, digits, dots, dashes and underscores before the path is built, so nothing a shell would read can reach the prompt. All three ways in go to one place: the selection toolbar's Paste, a hardware Ctrl+V (⌘V on a Mac layout) taken before xterm2's own shortcut so ^V and Ctrl+Shift+V still reach the shell, and Gboard's clipboard strip, which needed the terminal to name the picture types it takes — it named none, which is why that did nothing either. 20 MB, the file tab's own ceiling: what the provider declares is read first, so a video is refused before a byte is copied, and the copy is weighed again and deleted for a provider that declares nothing or lies. The native read is the one part no test could run, so the tablet is the first machine ever to execute it. asked for in "paste image di jeansh tidak langsung jadi upload file tadi aku coba di claude" |
| A host keeps a second address, so the tailnet going down is not a reason to edit it: connecting uses whichever of the two answers, and a failure that is a refusal rather than an unreachable host never quietly retries on the other one | Coordinator | e311277 | UAT passed on build 1.0.45+45, installed on the tablet on 2026-09-18 with its data kept ("5 pass"), with one fault the user found in the same breath, now the row above: the trust prompt named the saved address while the alternative was what answered. An Alternative address field under Host, shared with the saved address's port, user and credentials, being the same sshd. Connecting sends the saved address out at once and the alternative 250 ms behind it — a browser's happy eyeballs delay — or at once if the saved one has already failed, so an address that is up keeps its head start and a tailnet whose packets go nowhere costs a quarter of a second rather than a TCP timeout; the loser's socket is destroyed the moment it arrives, before a byte is written, so only the winner ever starts a handshake and a wrong password, a refused key or a host key that does not match is never retried anywhere else, and both failing throws the saved address's error. It sits at the one place a socket is opened, which is also the jump hosts' hop, so terminals, tmux, ssh -L, ssh -R, the notification reverse-forward, SFTP and the database tunnels inherit it without a line of their own. The host key was pinned against the saved address whatever answered, which is the fault the row above fixes; it is now pinned under the address that answered, as OpenSSH's known_hosts is. A toast says so only when the alternative won. No separate alternative port, on the grounds that a box reading 22 forever is one more thing to get wrong. asked for in "aku butuh juga di client setting perlu di tambahkan alternative ip, jadi ini kegunaanya ketika user di local dan vpn tailscale mati user tidak perlu edit clietn lagi untuk pindahkan ke local ip" |
| The Markdown preview comes back where it was left: Reload from host puts a spinner in place of the whole body and Source takes the preview out of the tree, so either one threw the rendered list away and built it again from the top, losing the reader's place; the preview now owns a scroll controller started from a number the tab keeps, written as it scrolls and never through setState, since rebuilding on a scroll would parse and lay out the document again, and a file that came back shorter settles at its bottom | Coordinator | fd6f027 | UAT passed on build 1.0.45+45, installed on the tablet on 2026-09-18 with its data kept ("2 pass"), asked for in "aku butuh ketika reload view render file md posisi terakhirnya tidak berubah itu bisa kah?" |
| Releasing from this machine rather than from GitHub: one local command builds the signed bundle and publishes it to Play’s closed testing track through the Developer API with a service account, with a dry run that stops short of the commit, a refusal to upload a build number Play already has, and no secret ever printed; the GitHub Actions workflow goes, the user having turned it down on cost | Coordinator | 02f2fec | on main and waiting for the user, one step on: the service account arrived on 2026-09-18 and sits at ~/keys/jeansh-play-service-account.json, mode 600 in a 700 directory, moved rather than copied out of /tmp so no key was left there, and checked as a service_account with a PEM private key without either being printed; it is wallpaper-app-deploy in project iron-asset-477319-k0, the WallpaperApp's, reused. A dry run built and signed the bundle (1.0.55, versionCode 55, CN=Jeansh), signed in to Google — so the key and the token exchange work — and then stopped at opening an edit with HTTP 403 "The caller does not have permission", before anything was created, so nothing reached testers: the account has not been granted Jeansh in the Play Console, which only the user can do. Before that: tool/release.sh --publish builds and publishes, but nothing can upload until a Play service account exists — made in Google Cloud with the Play Developer API on, granted Release apps to testing tracks in the Console, and saved outside the repo at ~/keys/jeansh-play-service-account.json; the first bundle still goes up by hand, Play refusing the API for an app that has never had one. The happy path is unverified end to end, since reaching it would publish to real testers: what is proven is that every refusal fires without credentials and that the JWT it signs verifies with openssl. It talks to Play with the standard library and openssl alone, no pip package, and a --dry-run does everything but the commit (asked for in "i need when build it will upload to goole playstore and autommatically create close testing" and "untuk pipeline build ini aku nga mau taruh di github ya karna berbayar dan mahal") |
| Keyboard input never goes dead: after the first hardware key the IME connection is shut for the rest of the process, so the terminal takes input only while its FocusNode holds focus, and a focus lost and not regained leaves no input path at all, neither IME nor hardware keys, which is why leaving the app and coming back revives it | Coordinator | 97b1f32 | UAT passed for now on build 1.0.51+51 ("scroll terminal, input keybiard mati untuk sementara fix dulu"), the user holding it open to come back if it returns; and honestly a net rather than a cure: the trigger was never found. Every path that could be driven from a test — switching tabs and back, closing a tab before the active one, the Transfers tab appearing, backgrounding and resuming, a dialog, the files drawer, a key-bar tap — restores focus by itself, so what empties the input on the tablet is either a path only Android can take (the file picker, a Custom Tab, split screen, the share sheet, a WebView) or not Flutter focus at all but the platform input connection, which is torn down inside key dispatch and never asked for again — and the user's own workaround, typing in another app first, is exactly the gesture that makes Android rebind it, which points that way. What is proven is that the hole is real and unhealable once reached: with focus parked on the enclosing scope and no node holding it, no key reaches the terminal at all, since the IME connection has been shut since 57b0bd6 and focus is the whole of the input path; a test that parks it there failed before this commit and passes after. So the pane now watches every hardware key through HardwareKeyboard, which is heard whether it has focus or not and runs before the focus chain, and takes focus back on the spot when it finds it parked on its own scope — that very key lands, not the one after it. It only ever takes focus from its own scope, so a dialog, a sheet, the files drawer and any field being typed into are untouched, and it watches without consuming. If the input still dies and still needs an app switch, the cause is the platform connection and the next step is to stop tearing it down inside key dispatch, which means a different answer to the doubling than the one that row settled on (a bug the user hit: "sama aku ada issue beberapakali input dari keyboard tidak terbaca aku perlu pindah ke applikasi lain dulu lalu masukkan input disana baru balik lagi ke jeansh baru bisa input") |
| Files drawer: Copy content on a long press, so a file reaches the clipboard without being opened, sharing the file tab’s helper, wording and 256 KB ceiling; the difference is that the text is on the host rather than in memory, so it has to be fetched, and the size is read from the listing first, refusing a big file before any of it is pulled down | Coordinator | ab8bbbb | UAT passed on build 1.0.51+51 ("copy content pass"), first on the tablet in build 1.0.38+38 (asked for in "point 4 di file tree juga di tambahkan ketika user click lama opsi copy content di tambahkan di situ") |
| Pasting into a terminal keeps its spaces: the IME buffer is padded with spaces so the space-bar trackpad has room to slide, and the code that worked out what was inserted walked in from the left while the text still matched that padding — that is, while it was a space — so any inserted run beginning with a space lost it and the window slid into the trailing padding; Gboard commits a paste a word at a time, each chunk arriving as a space and a word, so every space was eaten and the words fused, while chunks whose spaces fell in the middle came through unharmed, which is why it looked inconsistent | Coordinator | d820257 | UAT passed on build 1.0.45+45 ("6. sudah pass"), first on the tablet in build 1.0.38+38, a bug they hit pasting a paragraph ("kenapa ketika paste dari copy di terminal session jadi seperti ini ya, spacenya pada ilangan") |
| File tab: Copy content and Copy image in the ⋮ menu, so the whole file goes to the clipboard as text and a picture goes as itself, ready to paste into another app; the text half is Clipboard.setData, while the picture half needs a native ClipData holding a content:// URI from a FileProvider, Flutter having no clipboard API for an image, so a new ClipFileProvider serves it, unexported and reaching cache/clip alone, the hosts and keys and uploads staying unreachable, and Kotlin copies the picture there because the viewer keeps its own in code_cache, which no provider path can reach and which goes when the tab closes; a copy button also sits beside every code block in the rendered Markdown, copying that block without its fences or language tag, while a mermaid block deliberately gets none, being a diagram rather than text | Coordinator | a9c6c99 | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "di filetab aku butuh tambahkan sub menu untuk copy content jadi itu langsung ngecopy semua isi contentnya ke clipboard, dan juga perlu di tambahkan copy file gambar untuk langsung dimasukkan ke dalam clip board") |
| Database browser: nothing not saved goes without asking: with changes in the grid, Run (Ctrl+Enter too), a tap on another table, collection or key, Reconnect and the tab’s ✕ each ask "Discard N unsaved changes?" first, Keep editing beside Discard, and the tab strip asks the page itself through the key it already holds for it, since closing the tab takes the page and them with it; switching to another tab keeps them, the pages sitting in an IndexedStack, and the read-back after a save never asks, those changes being saved by then | database client ui development | 3118e00 | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "ketika user mau ganti halaman yang dapat menghilangkan changes yang belum di commit maka perlu ada notifikasi ke user") |
| Database browser: MongoDB and Redis rows edited in the grid too, with the same Save, Discard and read-back as PostgreSQL's: a MongoDB find's documents by their _id, which is locked (a value typed into a string field stays a string, anything else is read as JSON when it can be, a double stays a double, Set NULL sets null, and a new document leaves out the fields not given a value), each save one update, one delete and one insert in turn — MongoDB without a replica set has no transactions, so a refused first write makes none and keeps the changes, while one refused after another was made says so and reads the rows back; and the key a Redis view shows — GET's string (its time to live kept), HGETALL's hash (a field renamed too), LRANGE's list (by place, the # column locked), SMEMBERS's set, and a sorted set's members and scores from ZRANGE … WITHSCORES — each save one MULTI … EXEC, a score that is no number refused before anything is sent, no NULL, and no new row for a string | database client ui development | 1971a51 | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "tambahakan juga untuk redis dan mongodb") |
| Database browser: a PostgreSQL table's rows edited in the grid and saved all at once, as TablePlus does: when every column of a result is one table's and its primary key is among them, a tap on a cell edits it (Set NULL too), holding a row deletes or restores it, Add row puts a new row on top whose untouched cells take their default, and what is not saved shows in colour until Save (or Ctrl+S, ⌘S) makes every change in one transaction and reads the rows back — one refused change makes none, and its error says why; Discard, running a query or opening a table drops whatever is not saved; MongoDB and Redis are the row above | database client ui development | 7cef51a | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "perlu ada feature untuk melakukan edit data dari rdbms dan perlu ada satu button untuk save all changes jadi kaya commit, jadi selagi data belum di commit maka ketika data di reload changesnya akan di hapus, fitur ini mirip dengan yang digunakan di tableplus") |
| Images open in the file tab as pictures instead of binary noise: a file whose name ends .png, .jpg, .jpeg, .gif, .webp or .bmp opens in a viewer rather than the editor — those are the kinds Flutter decodes by itself, so .svg, .ico, .heic and .tiff deliberately stay editor files and keep the existing "This looks like a binary file" message, which already offers Download, instead of a viewer that could only fail; the picture fits the tab, pinch zooms and pans, and a double-tap jumps between the whole image and true device pixels anchored where it was tapped, with the name, the pixel size and the file size above it on a mid-grey ground, the one background a transparent PNG shows against in both themes; the file streams into a copy of the app's own a chunk at a time, through the same download every transfer uses, so nothing but the decoded image is ever held in memory, and anything past 20 MB is never opened — the size lands with the first chunk, so the rest is not pulled down — saying so and still offering Download, as do bytes that turn out not to be an image; the copy is deleted whenever the tab closes and the pixels leave Flutter's image cache with it, whichever of the close and the download finishes last; the tab chip still reads `<hostname> · file`, and a restored tab that named an image comes back as an image tab, because file tabs are saved as their path and the same rule routes them | Coordinator | d7c3140 | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "current file viewer only can view text, need support to open image") |
| Transfers run at the speed of the link again, not at the frame rate: last round's fix handed dartssh2 32 KB per event-loop turn to keep the frames coming, but the yield it used, a Future.delayed of Duration.zero, is a Timer, and a Flutter app services those about once a frame — so the real ceiling was 32 KB times 60, about 1.9 MB/s, which is exactly the 1.6 MB/s the user measured, and which the headless benchmark could never see because a Dart VM with no UI spins the loop about 175,000 times a second instead of 60; the socket now spends a 4 ms time budget each turn rather than a fixed number of bytes, so it hands over as much as the machine can take between frames and still yields honestly on a slow one, and uploads no longer wait on dartssh2's 16 pending writes, a separate gate of its own, but 64; measured while bound to a frame, a download goes 2.1 to 11.7 MB/s and an upload 10.4 to 37.6 MB/s on a link 30 ms away, with no frame dropped, and tool/transfer_bench.dart now imports the pacer instead of holding a copy of it, so what the benchmark measures is what the app runs | Coordinator | 36c599f, c09377d, f219ec7 | UAT passed on build 1.0.36+36 ("point 1, 2,4,5,6 pass"), at the third attempt, after failing twice, most recently on build 1.0.30+30: still about 1.5 MB/s and still laggy on a big transfer ("the download speed still only at 1,5MB, and user still feel laggy when user upload/download big file"), so the frame-rate ceiling this commit lifted is not what binds on the tablet; the build under test was a debug APK, where Dart runs in the JIT with assertions on and the pure-Dart cipher pays for it, and the tablet was moved to the profile build afterwards, AOT and installed over the top so its data was kept, since release signing would have forced an uninstall and taken the hosts and keys with it, and it was measured on the emulator against the same 100 MB file and server: the debug build gives 1.66 MB/s (16 to 35 percent in 12 s) and a profile build 3.5 MB/s (8 to 18 percent in 3 s), so AOT roughly doubles it and neither comes near the 11.7 MB/s the headless benchmark promised. The arithmetic says why: 1.66 MB/s is 27.7 KB a frame and 3.5 is 58 KB, one piece and two of the 32 KB the socket hands over, so a turn ends after however many pieces the device can decrypt in 4 ms and throughput is 60 times that. The time budget replaced a fixed 32 KB a turn with a number that on a phone works out about the same, which is why the tablet did not move. Raising the budget only trades frames for speed with no knee to sit on, so what was left was taking the transport off the UI isolate, which c09377d does: the whole SSHClient runs on an isolate of its own, with Dartssh2Transport unchanged inside it and IsolateTransport swapped in at the three places that built it, so the terminal, tmux control mode, ssh -L, ssh -R, the notification reverse-forward, SFTP and the database tunnels all keep the code they had. A transfer is one wire stream, so progress, cancel and a dropped connection ride the one mechanism, and a download never crosses the boundary at all, being written on the worker isolate; reading a credential, ruling on a host key and loading a jump chain stay on the UI isolate, a handful of messages while connecting and none after. PacedSocket is deleted, there being no per-turn budget left to keep. Measured frame-bound on a desktop, 100 MB: 34.4 MB/s with no frame past 16 ms, against 26.5 MB/s and 52 late frames with the client on the frame isolate, and 32.4 with the frame load off, so the frames cost it nothing. It is on the tablet now, in build 1.0.36+36, installed on 2026-09-18 with its data kept, so the tablet can settle it at last. Neither benchmark number is a device number, so the coordinator took one: on the emulator, against the same 100 MB file and server as the earlier runs, a profile build goes from 3.5 MB/s on the paced socket to 8.4 MB/s on the isolate (10 to 26 percent in 2 s), and the transfer runs to the end and hands the file to Android as before, with the terminal, the file tree and the database tabs all working over the new transport. That is 2.4 times, on the same hardware, and the ceiling is no longer 60 times what fits in 4 ms. The tablet is still what settles it for the user. It needs a full rebuild rather than a hot restart, the isolate entry points having to be in the APK, and any widget test that calls LiveSession.connect must now pass a transport of its own. It was fixing an earlier failed UAT ("yang tidak pass adalah ketika user melakukan transfer data somehow user mengalami laging dan speed maksimal bahkan local to local adalah 1,6MB") |
| Duplicate on a host card: the ⋮ menu on Home offers Duplicate between Edit and Delete and saves a copy of the host straight away, so the new card is on Home to edit if anything needs changing, the way Duplicate session on a shell tab acts at once rather than opening a form first; the copy is a host of its own, taking the whole profile through `copyWith` — address, port, user, auth method, jump host, tmux, file-tree root and every other field, so one added to HostProfile later travels with it instead of being silently dropped, which a test checks by comparing the two profiles whole — and taking the password, private key and passphrase across to its new id, so it connects without the secret being typed again and nothing secret is ever logged or shown; what belongs to the original stays there, the copy having no open session, no history of its own in the logs and no notification key until it first connects, since all three are held per host id; it is named `<host> (copy)`, or `(copy 2)` and on while that name is taken, compared against the names the cards actually show | Coordinator | 21ced14 | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "tambahkan duplicate di host card") |
| Database icons are each product's own brand mark: PostgreSQL's elephant, MongoDB's leaf and Redis's stack, drawn from the devicon block already in the bundled CaskaydiaCove Nerd Font (U+E76E, U+E7A4, U+E76D, found by rendering the whole U+E700-E7C5 block and looking at it rather than guessing a codepoint), so the marks the user asked for as SVG cost no new asset and no flutter_svg dependency and arrive the same way the host OS logos already do; each is white on its brand's own colour (#336791, #47A248, #DC382D, every one at least 3:1 against white, so it reads in either theme) in a rounded square the size of a host's OS badge, which makes a database card and a host card read as one family on Home, and the same mark shows at 16 px on the database tab chip and beside each name in the editor's type picker | Coordinator | ca90b9f | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass") (asked for in "icon database perlu pakai icon svg berdasarkan brannya, postgres, mongodb dan redis") |
| A way into the Transfers tab without a transfer, and the lag measured to its floor: Home's app bar gets a Transfers button beside Port forwarding, Logs, Known hosts and Settings — where the known hosts entry point was moved to — opening the same tab a notification tap opens, so asking again goes back to the one tab instead of making a second, and with nothing ever downloaded it opens on "Downloads and uploads show here."; the lag was measured before anything was changed, and then nothing was, because the numbers leave nothing cheap to take: one 32 KB packet costs 494-525 µs to open in the JIT a debug build runs and 932 µs in AOT (a release build is slower here, not faster), against an event-loop turn of 1.6-2.6 ms p99 through a paced download, so the cipher is about a quarter of a turn and SSH framing, SFTP parsing and the file sink are the rest of it; tool/transfer_bench.dart now also opens packets on a worker isolate holding the cipher (45.1 against 59.8 MB/s, taking 0.51 ms off this isolate and adding 0.18 ms of hand-over for it) and through an Isolate.run each (23.0 MB/s, far worse), and neither can take the framing along, because a connection's cipher is one running state every channel shares — so only the whole transport on an isolate of its own, or the second connection the last round turned down, moves what is left, and which of those to build is the user's call; reading 32755 a time rather than 32768, the one setting the first run hinted at, loses 18% on a fast link and stalls worse on a slow one, so it stays as it is | Coordinator | 2a52dfd | UAT passed: the button first ("tombolnya sudah pass") and the transfers at last on build 1.0.36+36 ("point 1, 2,4,5,6 pass"), having failed here as laggy and capped at about 1.6 MB/s even between local machines ("yang tidak pass adalah ketika user melakukan transfer data somehow user mengalami laging dan speed maksimal bahkan local to local adalah 1,6MB"), which the coordinator traced to last round's pacing — the socket hands dartssh2 32 KB per event-loop turn, and a UI app's loop runs about 60 turns a second, so roughly 1.9 MB/s is a ceiling the headless benchmark could not see; being fixed by pacing on a time budget instead; the lag was measured again and deliberately left alone — with the paced socket the stalls are 1.6-2.6 ms p99 with no janky frames (7.5-14.5 ms before), the cipher is only about a quarter of an event-loop turn, and a worker isolate holding it buys back about 0.5 ms of 2.4 ms while costing about 25% throughput and cannot take dartssh2's framing with it, so moving the whole transport to an isolate or opening a second SSH connection for transfers is the user's call (asked for in "tab transfer juga sudah pass,  lagi nya masi terasa ini better di lakukan di background thread, sama perlu ditambahkan entry point manual untuk buka history download ini, karna selain trigger upload/download user tidak bisa masuk ke tab tersebut") |
| The terminal falls back to a bundled symbols font for the glyphs a code font lacks: Claude Code's status line writes `⏵⏵ auto mode on` with U+23F5, which none of the five bundled fonts has, and the tablet holds no copy either — Android ships Noto Sans Symbols subsetted and that codepoint was dropped — so both triangles drew as empty boxes; a 175 KB subset of Noto Sans Symbols 2 2.008 (SIL OFL 1.1, its OFL.txt beside it) now stands behind the Nerd Font in every terminal's fallback chain, ahead of xterm2's own, carrying Miscellaneous Technical (U+2300-23FF, the ⏴ ⏵ ⏶ ⏷ ⏸ ⏺ a status line draws), arrows, geometric shapes, miscellaneous symbols, dingbats (✔ ✗, of which every code font has a handful) and braille (which JetBrains Mono and Fira Code have none of, so a braille spinner drew as boxes in them too): 668 of the font's 2955 glyphs, cut with pyftsubset, 175 KB of 1.2 MB; U+23F5 comes out 0.586 em wide, Cascadia's own cell to three decimals, so the grid lines up | Coordinator | 99dfe09 | UAT passed ("simbol untuk glyph sudah pass") (asked for in "ada font yang missing di samping auto mode on") |
| The key bar's ALT and CTRL reach the magic key and the bar's own keys: an armed modifier folded only into typing, so ALT with the magic key's Enter sent a plain CR where a new line wanted ESC CR; a key of one character now takes the armed modifiers wherever it is pressed (`\r` becoming ESC CR, a letter its C0 byte), while a ready-made sequence — a cursor key, ESC f, a custom key's own combination — is left alone, carrying its modifiers already; folding disarms once, the way it does for typing, and a sequence left alone keeps the modifier armed for the next key | Coordinator | 2c7f389 | UAT passed ("kombinasi keybar dan magic fab sudah pass") (asked for in "keybar + magic key enter harusnya bisa di gabung, misal kalau aku togle alt + enter harusnya jadi new line") |
| No hardware key arrives twice, whatever the combination: with a hardware keyboard the soft keyboard's IME connection was open as well, and Android gives every key to the IME before the app sees it — an IME that does not want a combination passes it on with InputConnection.sendKeyEvent, which Flutter feeds straight back into its own keyboard manager while the platform delivers the same press to the view as well, so xterm2 read one press twice and sent its bytes twice (two ESC CR for Shift+Enter, two 0x14 for Ctrl+T), and an IME can also commit what it read into the editing buffer, as Flutter itself does with any key the framework leaves unhandled; the connection is now the soft keyboard's alone — the first hardware key seen anywhere in the app shuts it and keeps focus from opening another, an IME edit or editor action arriving without one is dropped, and a plain ask (a tap on the terminal) opens it again — so the soft keyboard, composing IMEs, the space-bar caret slide, backspace, the key bar and the magic key are all untouched, and 0b6f03d's '\n' strip goes with the diagnosis it belonged to, a multiline soft-keyboard paste keeping its line breaks; Shift+Enter stays ESC CR, which nano does not know and answers with `[ Unbindable key: M-^M ]`, a status line with nothing lost, chosen by the user over a setting or a plain CR | Coordinator | 0b6f03d, 57b0bd6, aa5110f | UAT passed on build 1.0.36+36, installed on the tablet on 2026-09-18 with its data kept ("point 1, 2,4,5,6 pass"), after a UAT failure on 1.0.22+22 ("input ganda masu kejadian") whose cause is now found and fixed in aa5110f: Claude Code turns the kitty keyboard protocol on with flags 1 and 4 (ESC[>5u) and leaves out flag 2, the one that asks for key releases, so xterm2 encoded every release with nothing to mark it as one and sent it as the press over again — a single hardware Ctrl+T pinned a session in Claude Code's list and unpinned it about 110 ms later as the key came up, and Shift+Enter made two new lines the same way; aa5110f drops a release unless the program asked for releases, wrapping the input handler at the one place every terminal and every tmux pane is built, with a test that failed before it. The key bar was never affected, since it sends bytes and has no release at all, which is why ^T from the bar always worked. A cat -v check by the coordinator had shown one clean ^T and wrongly cleared the app, because cat -v never turns the kitty protocol on and so never reached the faulty path — the user had found the same thing on the tablet. Found by the keyboard input ^t handler session in the user's screen recording; the IME doubling this row began with is a different fault and its fixes stay. Needs an APK install, or a hot restart at the very least, since a terminal already open keeps its old input handler; after an earlier failed UAT: "sepertinya double input ketika klik kombinasi key di keyboard bukan cuma terjadi di <shift>+<enter>, tapi terjadi juga di <ctrl>+<t> aku tidak tau apakah ini terjadi umum atau tidak" and "tapi kenapa claude newlinenya jadi ketangkap 2 kali ya kalau pakai keyboar" on build 1.0.15, which held 0b6f03d; 57b0bd6 (build 1.0.16+16) fixes the cause and waits for the tablet (asked for in "shift+enter jadi ke double enternya bukan cuma 1 kali enter"; nano's message asked about in "ketika klik shift+enter di nano dapat error itu ntah apakh kombinasi itu sudah sesuai atau tidak", answered by leaving ESC CR as it is) |
| Database browser: a Table / JSON switch beside Run shows a result's rows as JSON, a card a row (a MongoDB document as it is, a PostgreSQL or Redis row as its columns and values, a NULL as null), laid out as a tree: a nested object or array is one line that opens to what is in it (`owner {2 keys}`, `tags [5 items]`, the first 100 of a long one and a count of the rest), MongoDB's $oid, $date and the like stay one value, values are selectable, and each card's Copy JSON copies the whole row | database client ui development | 2c610f6, 02330e9 | UAT passed ("table json switch sudah pass") (asked for in "di dbms view user bisa mengubah list data yang di tampilkan dari table menjadi list json" and "the json view it will expand able like a tree view if the document have nested value") |
| Transfers tab and progress notifications, and a download that keeps the frames coming: every download and upload is a transfer in one Transfers tab for the whole app (newest first, with the file, the host, which way, how much of it, how fast and how it went; Cancel while it runs, Open for a finished download, Clear finished), which joins the end of the strip when a transfer starts without taking the screen, and leaves what runs alone when closed; each transfer is also a notification on a low-importance channel of its own, with a bar, the percent and the size and Cancel, updated at most once a second and alerting once, becoming "Downloaded <name>" or a failure, and gone when cancelled, a tap opening the tab; the lag was dartssh2 decrypting and framing all one socket read brought in a single event-loop turn on the UI isolate, with half a megabyte in flight, so the socket now hands it 32 KB a turn (stalls of 16-24 ms on a desktop become 2-4 ms, at no cost in speed), uploads stream through dartssh2's own writer with 16 writes in flight (10.7 against 7.4 MB/s on a link 30 ms away), and the pages no longer rebuild once a chunk, about 4 ms each in a debug build, only their bars do | Coordinator | 481538a | UAT passed ("tab transfer juga sudah pass"), with two follow-ups the user asked for in the same breath, now their own row at the top: the lag still being felt ("lagi nya masi terasa ini better di lakukan di background thread") and no manual way in ("sama perlu ditambahkan entry point manual untuk buka history download ini, karna selain trigger upload/download user tidak bisa masuk ke tab tersebut") (asked for in "separated download/upload tab mana?, dan ketika lagi proses download/upload harusnya ada notifikasi seperti yang browser punya lalu tadi aku mengalami laggy selama proses download") |
| Tabs survive an app restart, like a browser's: the open tabs (each terminal's host and tmux session name, its files and web pages, and the database tabs) are saved as they change, with nothing secret (a web page by scheme, host, port and path only, never a Tailscale sign-in), and come back when the app starts, unconnected; a terminal tab connects the first time it shows and its files come back once it has, and a web or database tab loads only when first shown; closing the app no longer ends tmux sessions, only a tab's ✕ does; a restored tab whose tmux session is gone from the host says so and offers Start a new session | database client ui development | 9754567 | UAT passed ("tab bertahan setelah restart app sudah pass juga") (asked for through the tmux question's agent: tabs that stay after the app is closed and opened again until the user closes them, like a browser, with a notice when the tmux session is gone from the host; started on "go") |
| Never two live copies of Jeansh: whatever starts a second MainActivity (a tap on a stale Jeansh card in Recents, which Android restores as the root of its old task; a launcher or notification tap over a file picker or Custom Tab; a floating window, split screen or "new window" started as a task of its own; a sshbox:// link fired inside another app), the copy finds the running one through a process-wide reference, brings its task to the front, hands it the intent through onNewIntent (a link, notification tap or share still arrives; a card from Recents replays nothing) and finishes before it runs any Dart, taking its own empty task out of Recents; the old launcher-only guard is now part of it, a relaunch after a config change isn't a copy, and one floating window still works | Coordinator | b3b24d7 | UAT passed ("tidak boleh ada 2 instance jeansh juga sudah pass"), after the coordinator's re-check at the 1.0.22 install: the background-apps list held one Jeansh card before the install, after it with the app killed, and after opening it from the launcher icon; checked first on the emulator, where tapping a stale Jeansh card in Recents had started a second live copy and now leaves one; the user found how the two copies appeared — after a reinstall, the background-apps list shows two Jeansh entries ("tidak boleh ada 2 instanace ini aku sudah tau cara reproducenya adalah, dengan cara cek background apps jika terjadi install ulang maka akan ada 2 app jeansh"), which is two cards in Recents rather than two live copies (asked for in "sama tadi aku menemukan aku bisa buka app jeansh ada 2 instance harusnya ini tidak bisa, tapi aku nga tau cara reproducenya sampai ada 2 instance jeansh") |
| Database editor: Import URI fills the type, address, port, user, password and database from a pasted postgresql://, mongodb:// or redis:// URI (percent-decoded; MongoDB's authSource, and a replica set's first host; a URI with no password keeps the one typed; mongodb+srv:// and rediss:// are refused with why) | database client ui development | 12c3f4f | UAT passed ("import uri sudah pass") (asked for in "tambahkan user bisa import uri untuk di parse ke dalam field yang ada") |
| Mermaid diagrams in the Markdown preview: a mermaid code block draws as its diagram (sequence diagrams, flowcharts and every other kind Mermaid knows) from a bundled Mermaid 11.17.2, offline, light or dark with the app; a wide diagram scrolls sideways, one Mermaid can't read shows Mermaid's error and the source, and other code blocks and Source are unchanged; the source is untrusted, so Mermaid runs strict with links removed, and the page loads nothing but its own scripts and navigates nowhere; docs/notification-architecture.md already draws its 8 diagrams in Mermaid (7 sequence diagrams, 1 flowchart) | Coordinator | 4b3d699 | UAT passed ("mermaid di markdown pass") (asked for in "markdown viewer belum support mermaidjs ya? kalau belum di develop featurenya dan /home/triasbrata/dev/sshbox/docs/notification-architecture.md ini pastikan squence diagramnya pakai mermaidjs") |
| Notifications can't be sent to the wrong phone: the app signs each register with the host key it registers (X-PARTNER-ID, X-TIMESTAMP, X-EXTERNAL-ID and X-SIGNATURE, as for a send), so a public key alone can't point a host's pushes at another phone, and the relay refuses an unsigned register; a revoke counts as done only on 204 or 401 "unknown key", and a key dropped with its host or by a reset goes to no host at once and waits in the secret store until the relay confirms, tried again at each launch, host delete and reset; a revoke is never rate-limited | Coordinator | 35a66b4; jeansh-notify a686159 | UAT passed ("notifikasi tidak bisa ke device yang salah pass"); the coordinator had also checked the relay end at the user's request: with a throwaway key, an unsigned register, a register signed by another key and a register whose X-PARTNER-ID isn't the body's key id are all refused 401, a correctly signed one reaches FCM, and a send from an unregistered key gets 401 unknown key (asked for in "oke aman aja yang penting jangan sampai notifikasinya salah kirim"); the relay went live the same day with jeansh-notify a686159, and an unsigned register now gets 401 |
| Big downloads and uploads no longer freeze the app: every SSH connection prefers chacha20-poly1305, then AES-CTR, with AES-GCM last, since dartssh2's first choice, AES-GCM in pure Dart, ran at about 1.3 MB/s on the UI thread (some 30 times slower) and held it until Android said "isn't responding"; a download streams into a file of the app's own (512 KB in flight) and only its path goes to Android, which asks where to save it and copies it there off the main thread; the copy is deleted whatever happens, and the 100 MB limit is gone | Coordinator | b7b21f1 | UAT passed ("download dan upload  file besar sudah pass, tapi ada catatan tadi"; the notes — a Transfers tab, progress notifications and the lag during a download — are the Transfers row at the top) (asked for in "when user download big file like /home/triasbrata/dev/sshbox/build/app/outputs/bundle/release/app-release.aab applikasi langsung hang dan seperti ini /tmp/Screenrecorder-2026-09-15-00-50-46-835.mp4 ketika upload juga ngalamin yang sama, apa mungkin ini karna di kerjakan di ui thread jadinya bikin ui lagging") |
| Database browser for PostgreSQL, MongoDB and Redis: saved databases are listed on Home under the hosts, and Home's one Add button offers Host or Database above it; each database is reached through a saved host's SSH connection (like `ssh -L`, jump hosts too), opens in a tab of its own beside the terminal and file tabs, lists its tables, collections or keys, and runs SQL, database commands or Redis commands with the result in a grid | database client ui development | c06f951, 362cc84 | UAT passed ("database browser sudah pass"), after a failed UAT: "fab di home di rombak saja jadi Add saja, ketika di click di atasnya akan muncul host atau database", "list database adanya di home juga" and "ketika user open connection database harusnya itu masuk ke tabbing system yang sudah ada sama seperti file, dan terminal session" (asked for in "develop rdmbs ui for mongodb, redis, and postgress") |
| Share with Jeansh goes to the session of the Jeansh already running instead of starting a second copy: the SEND filters move from MainActivity to a new ShareActivity (no window, never in Recents), which forwards the share to Jeansh's own task (NEW_TASK finds it by MainActivity; CLEAR_TOP with SINGLE_TOP hands it to onNewIntent, closing a file picker or Custom Tab left open over it) and finishes; with Jeansh not running it still cold-starts through `pending`; MainActivity stays singleTop, because singleTask would close a picker or Custom Tab on every launcher tap and still make a copy for a sender that asks for a result; and a launcher tap that Android answers with a second MainActivity on top of Jeansh's (a file picker or Custom Tab open over it, after a share or a notification tap that cold-started the app) finishes that copy in onCreate before it runs any Dart, so the task shows as it was | Coordinator | 65d4255, 7481f81 | UAT passed ("share with jeans sudah pass"; asked for in "tadi ketika share with jeansh, applikasi jeansh malah spawn applikasi baru bukannya upload ke existing session di applikasi yang sedang berjalan. berikut videonya /tmp/Screenrecorder-2026-09-15-00-41-34-998.mp4") |
| Signed relay notifications, SNAP-style: the app makes one ECDSA P-256 key per host, registers only the public key with the jeansh-notify relay, and passes the private key to that host as LC_SSHBOX_KEY; servers sign each send (X-PARTNER-ID, X-TIMESTAMP, X-EXTERNAL-ID, X-SIGNATURE over method, path, body hash, timestamp and external id), and the relay checks the signature, the time window and replays, and takes the host from the key; deleting a host revokes its key, and the bearer relay key and LC_SSHBOX_TOKEN go away | Coordinator | 493ba25; jeansh-notify a493c69 | UAT passed ("ini sudah pass, tapi aku mau make sure coba kamu gambarkan aku arsitekturnya seperti apa dalam sequence diagrm di md file"; the architecture is in docs/notification-architecture.md): the relay is live at https://jeansh-notify.brata.cloud (asked for in "domain using jeansh.brata.cloud ok"; the workers.dev address is off) and build 1.0.0+3 is installed (asked for in "kita perlu ada mekanisme ketika ngirim notifikasi ke device user via relay cf worker, user harus pegang key dan payload itu harus di sign pakai key tersebut. dan key itu langsung di passing ke variable key ketika connect ke session tersebut. dan bodynya juga harus ada body validate signature  mirip seperti protokol SNAP di payment gateway. jadi device tablet register key yang valid, nanti server pakai key yang valid itu untuk memvalidasi payload dan server pengirimnya") |
| Custom key picker: Clear resets the picked key and modifiers; a fourth modifier, Super/Cmd (CSI modifier 8 on special keys, CSI u on the rest); a PC or macOS layout (⌃ ⌥ ⇧ ⌘, and on macOS ⌘←/→, ⌥←/→, ⌘⌫ and ⌥⌫ edit text like iTerm2's Natural Text Editing), remembered and saved with each key | Coordinator | 5c89335 | UAT passed ("custom key picker pass"; asked for in "add keybar sudah sesuai dengan yang aku mau cum tinggal tambahkan reset key yang user pilih, sama ini kurang key super/cmd di mac. ini perlu di tambahkan opsi layout juga untuk layout macos") |
| App version on every commit, like blurface-app: a pre-commit hook bumps the +N build number in pubspec.yaml when app files change and tags the commit [build vN]; a local release script and a GitHub Actions workflow build the signed AAB and send it to Google Play's internal track; store/ holds the listing, privacy policy and release notes | Coordinator | b04854c, bf509d3, 541ba0e, 39e53e4, 71c436e | the hooks since retired for CI's numbering, see the Versioning row; before that: tooling on main and the hooks switched on (core.hooksPath .githooks); the release script passed the user's review ("aku sudah baca dari release script dan aku oke sih") and finds keytool through JAVA_HOME or flutter doctor when it isn't on PATH (39e53e4); the upload key is made: blurface-app, whose key.properties the user asked to reuse ("untuk key.properties samakan saja dengan yang di punya sama blurface"), has only the Android Debug key, which Play refuses, so the coordinator made a new one at the user's request ("kalau tidak bisa baut baru saja kamu generatekan semua nanti prepassnya kamu taruh di ~/googleplaypreepass.txt"): ~/keys/jeansh-upload.jks (alias upload, CN=Jeansh), its password in ~/googleplaypreepass.txt, and android/key.properties, which git ignores; tool/release.sh built build/app/outputs/bundle/release/app-release.aab, 1.0.0 (3), 70.8 MB, signed by CN=Jeansh; the version name now follows the build as 1.0.N+N, 1.0.0+13 becoming 1.0.13+13 (71c436e; asked for in "build code harusnya 1.0.13 dong bukan 1.0.0+13"), and the user confirmed it on the tablet ("version 1.0.15 sudah  pass"); waiting for the user: the Play Console app and its first upload by hand, a Play service account, and the GitHub secrets (asked for in "aku butuh kamu buatkan setiap commit untuk update app version seperti yang dipunya di @../blurface-app/ lalu buatkan aku release appnya untuk di upload ke gplaystore, terus buatkan aku script pipeline untuk release app") |
| File tab: Download in the ⋮ menu saves the open file to the phone through the files drawer's save dialog (the server's copy; it asks first when there are unsaved edits; works in Preview and Source) | Coordinator | d9fc6bf | UAT passed ("download form file tab pass"; asked for in "perlu tambah lagi di tab file untuk bisa download file yang lagi aktif di tab tersebut") |
| tmux found outside a non-login PATH: the app looks for tmux on PATH, then in Homebrew (/opt/homebrew/bin, /usr/local/bin), MacPorts, Linuxbrew, Nix, ~/.local/bin and snap, then in the login shell's PATH, and runs every tmux by that full path | Coordinator | f79322d | UAT passed ("tmux mac pass"; asked for in "ini untuk deteksi tmux sepertinya ngebug kalau aku coba di macos yang install tmux di /opt/homebrew/bin/tmux di anggap tidak ada tmux di servernya") |
| Files drawer: Upload here… on a folder (several files at once; Replace, Keep both or Skip when a name exists) and Download on a file, saved to the phone through the system save dialog (up to 100 MB; bigger files point to scp) | Coordinator | 285255a, 268c2cb | UAT passed ("file explorer upload dan download pass"; asked for in "sama tambahkan di file explorer untuk bisa upload to directory dan download file") |
| Markdown preview in the code editor: a .md file opens rendered, and a toggle switches between the rendered view and the source (unsaved edits show in the preview; links open in a web tab; images show their alt text; the first 100 KB of a big file) | Coordinator | 1070a36 | UAT passed ("markdown preview pass"; asked for in "sama aku perlu pengerjaan untuk parsing view md jadi kalau file type md di open sama user di editor user bisa toggle untuk view parsed atau source dari md file") |
| Server notifications, direct: each SSH connection forwards a port on the server's loopback back through the connection (like ssh -R), the app answers it with a small HTTP handler and shows the notification itself, with no FCM; servers curl LC_SSHBOX_NOTIFY_URL with LC_SSHBOX_NOTIFY_SECRET, no binary to install | Coordinator | e0f33e6 | UAT passed ("untuk notification direct sudah pass", after "server notification  direct way kamu coba kirim sekarang"); the coordinator had checked it end to end over Tailscale SSH at the user's request ("server notification  direct coba kamu lakukan"): curl got {"ok":true}, notify.sh sent directly, a wrong secret got 401, and both test notifications reached the tablet (asked for in "atau kita open port saja dari situ system nanti curl ke port yang kita open dan applikasi langsung direct tampilkan notifikasi tanpa bantuan fcm", "jadi ada 2 cara untuk mengirim notifikasi, yang pertama direct lewat open port dan berarti applikasi akan buat light http engine lalu cara ke 2 lewat fcm dari cf worker") |
| Server notifications through FCM from a Cloudflare Worker: the jeansh-notify relay (public repo triasbrata/jeansh-notify, running at jeansh-notify.triasbrata.workers.dev while the app calls jeansh-notify.brata.cloud) holds the Firebase service account as a secret; the app registers for a relay key and passes it as LC_SSHBOX_TOKEN instead of the FCM token, Settings → Reset notification key cuts off every server, and the Go sshbox-notify binary gives way to curl (the repo's notify.sh tries the direct way first) | Coordinator | e0f33e6; jeansh-notify ed9f998, e11362e | replaced by the signed relay row at the top: the bearer relay key and LC_SSHBOX_TOKEN are gone; the Firebase key has been the relay's secret since 2026-09-14 ("read aja langsung dari file json credentialnya"), and the relay reaches FCM (asked for in "aku butuh distribute sshbox-notify supaya bisa di install para pengguna di local mereka bantu aku distribute binnarynya itu, sama itu kayanya kita pisah aja repository-nya jadi public repository", "hmm... benar juga sepertinya itu nga aman deh, apa kita buat kirim fcm-nya lewat worker cf dan? dan dari local cukup call api dari worker cf?", "jadi tidak ada perlu install binarry gitu kan ya?") |
| macOS app and an Apple build pipeline: `tools/build_apple.sh` builds on a Mac the macOS app (.zip and .dmg, with the app icon), an iOS simulator build and an iOS device .ipa (unsigned, or signed with `--team`); secrets on macOS go in the login keychain | apple build pipeline | 45cb035 | in development ("aku butuh kamu untuk build script pipeline yang bisa di jalankan di macos untuk build versi macos dan ios") |
| Jeansh promo website in a new private repo, triasbrata/jeansh-web: one page (the tagline, a terminal drawn in HTML, the features, "coming soon to Android and iOS") built with TanStack Start and shadcn/ui, on a Cloudflare Worker that deploys from GitHub through Cloudflare's GitHub integration | Coordinator | jeansh-web 2fdb839, d4b3ab4 | UAT passed ("promo website pass"), live at https://jeansh.brata.cloud (asked for in "jeansh.brata.cloud"; the workers.dev address is off); built with bun, and the GitHub connection in the Cloudflare dashboard still waits for the user, since only they can approve Cloudflare's GitHub app (asked for in "aku butuh kamu buat repo baru untuk website promosi dari jeansh ini. nanti buatnya pakai tanstack start dan shadcn/ui dan deploy ke cf, lalu di cf pasangkan integrasinya ke github") |
| Key bar custom keys: Add key offers the built-in keys not in the bar, a divider and Custom key… (an on-screen keyboard with Ctrl, Alt and Shift toggles picks the key combination and fills in the label, editable later; keys saved as text before keep working); each row's show/hide switch becomes a remove button | Coordinator | 52553c7, 929e2bb | UAT passed ("add keybar sudah sesuai dengan yang aku mau"; Clear, Super/Cmd and the macOS layout are their own row at the top), after a failed try: removing keys and putting built-in keys back were right, but a new custom key asked for what it sends as plain text ("key bar custom keys sudah pas cuma ketik tambah baru user tidak bisa diminat masukkan sends dengan inputan plain text, tapi kita harus buatkan either full in screen keyboard beserta combinationnya atau kita sudah sediakan combination yang tinggal di pilih sama user"; first asked for in "key bar settings pass, but user cant add more custom keys, need user can add custom keys, and replace the toggle to remove") |
| App icon from the user's third picture, for Android (legacy and adaptive) and iOS (every AppIcon size, full-bleed with no alpha); the notification silhouette is unchanged | Coordinator | f3a79eb | UAT passed on Android, checked on a release build ("oke pass untuk app icon"); iOS not checked yet, it needs a Mac (asked for in "aku butuh semua icon di ganti ke sini, dan persiapkan juga untuk menjadi icon dari ios /tmp/30171dbf-b31b-4ad4-b08b-ae4fce46a5b2.png") |
| Key bar settings: Settings → Keyboard → Key bar lists every on-screen key and divider with a show/hide switch and drag to reorder, a live preview and Reset to default; the files and upload icons stay fixed at the front | Coordinator | 9b4be2d | UAT passed ("key bar settings pass"; custom keys and removing instead of hiding are the row above) |
| Every message shows as the new toast, with no old grey snackbars left; toasts are plain light or dark following the terminal theme, show their type by an icon with no label, grow up to 80% of the window for long text, and let taps beside the card through | Coordinator | 1785796, d46f199, 2f98caa | UAT passed ("plain toast with type icon upto 80% wide pass"; asked for in "toast nya ini kenapa tidak pakai toast engine yang baru?", "toastnya tidak usah berwana warni, cukup light and dark saja sesuai dengan theme dari terminalnya" and "cukup tambahkan icon warning, information atau yang lain untuk membedakan setiap message type, tapi ada juga yang tanpa text. notification max window adalah 80% dari window jadi kalau ada text panjang gunakan saja maksimal windownya") |
| Notification icon: every notification (the session keep-alive, a push with the app open, and a push in the background) shows a white silhouette of a terminal in a jeans pocket instead of a filled square | trigger notification env_file | 1529a5b | UAT passed ("notification icon pass"; asked for in "notif sudah terkirim, cuma perlu fixing icon di notificationnya") |
| Tailscale sign-in from the connect sheet opens in a web tab again: Open link closes the sheet, the session gets its tab with the sign-in page beside it, and the page closes itself once signed in | Coordinator | 1ed6757 | UAT passed for now ("tailscalel sing-in in a web tab pass for now because i can re-produce."; after "login link tailscale belum open browser in tab", when the connect sheet had sent it to the phone's browser) |
| Push token passed to each host on connect as LC_SSHBOX_TOKEN and LC_SSHBOX_HOST_ID (tmux too) and read by sshbox-notify; the Home key button leaves, and Settings keeps "Copy notification token" as a fallback | Coordinator | 9f6df5b | UAT passed ("echo $LC_SSHBOX_TOKEN $LC_SSHBOX_HOST_ID ini pass", then "notification token sent to server already  pass") |
| App renamed to Jeansh with the tagline "Terminal buddy in your pocket" under the Home title, and a new launcher icon from the user's denim-pocket-and-terminal picture, legacy and adaptive | Coordinator | 64e873b, 417fb09, 414d352 | UAT passed (the launcher name and the Home tagline: "oke pass point 1 dan 2"; then, after the icon was swapped for the user's second picture, "ganti iconnya pakai ini /tmp/Rectangle_18__1_.png": "new icon pass dan namanya pass") |
| Known hosts, opened from the fingerprint button on Home: every trusted host fingerprint listed (address, the saved hosts using it, fingerprint), and Forget drops one so the next connection asks again | Coordinator | a9ea390, af2a44b | UAT passed (after the entry moved from Settings to Home: "entry point known host pindahkan ke home") |
| Connect sheet: a new connection or reconnect starts in a bottom sheet that shows progress, the host fingerprint to trust and the Tailscale sign-in link (opened in the phone's browser); a new tab opens only once connected, and closing the sheet gives up | Coordinator | a48d875 | UAT passed |
| Host editor: Choose file reads a private key into its field (OpenSSH/PEM, 64 KB max; a public key or .ppk is refused with a hint); passphrase and password get a show/hide eye | Coordinator | 80a145e, c4fc025 | UAT passed |
| Theme contrast: stronger colours for every named theme (terminal text 7:1, ANSI colours 4.5:1, dim grey 3:1; app colours more contrasty and truer to each theme's accent) | Coordinator | 53c761e | UAT passed |
| Logs: a history of past sessions (date, start–end, host with its distro icon, Saved bookmark), opened from the host list, like Termius | Coordinator | 6ce6441 | UAT passed |
| Server OS info: read on every connect (os-release, uname, macOS, Windows) and saved on the host; the host list shows it with a distro/OS icon | Coordinator | db1c6b0, ba6d9c2, d80018f, 7260967, b74a50f | UAT passed (after three failed tries: "yang server info ini ngebuat card sizenya tidak konsisten", uneven cards; "mending tulisan Ubuntu 22.04.5 LTS dipindah di bawah logonya saja … arch version di takeout saja"; "tulisan ubuntunya di remove aja kan udah ada iconnya itu sudah cukup sih" and "terus tulisan versionnya di kecilkan lagi", so now only the version, in smaller text, under the logo) |
| Host list as cards: a grid, 1 column on a phone and 3 on a tablet, not full width | Coordinator | 4bf1e4a | UAT passed |
| The host-list tab chip reads "Home" with a home icon | Coordinator | e93419e | UAT passed |
| Port forwarding on its own screen from Home: a setting picks a saved or new host and runs a headless SSH connection; each port is Tablet → Remote (`ssh -L`, 127.0.0.1 on the tablet) or Remote → Tablet (`ssh -R`), with one port field, common-port snippets (PostgreSQL, MySQL, Redis…), a live sentence and Advanced host/port | Coordinator | d8b0fe1, 9d719a0, 158e1f8, 17c5a90, 42ad695 | UAT passed (after three failed tries: "untuk portforward ini buatnya headless session, jadi perlu ada screen tersendiri …"; then "ini aku nga ngerti cara pakainya, ux nya mending di buat pilih arah port forward apakah dari tablet ke remote, atau remote ke table, lalu port yang mau di forward  dan ada pilihan advance untuk mengisi mengcustom port atau hostnya" and "user juga di kasikan port snippet, misalkan dia mau connect untuk postgrest"; then "harusnya untuk bagian advance ketika user sudah pilih snippet di bagian advance juga langsung terpilih", so a snippet now fills Advanced with real values) |
| Theme in Settings: light/dark/system mode and named colour themes (Clode, Dracula, Nord, Gruvbox, Solarized, Catppuccin, Tokyo Night, One Dark) for the app and the terminal | Coordinator | f3ad854, 73026d6, 627d7a1 | UAT passed ("theme, sudah pass tapi color palettenya kurang kontras"; the contrast follow-up is its own row at the top; after two failed tries: "theme and collor pallet yang aku maksud bukan cuma applikasi tapi di color schema terminalnya juga harusnya berubah juga", then "untuk theme ini kayanya sama aja", where switching palettes changed only a faint terminal tint and the cursor) |
| Long-press a tab that failed to connect → Close tab (its ✕ is Reconnect then, so it could not be closed) | Coordinator | 6c77828 | UAT passed |
| Security hardening: a new host key asks before it is trusted (fingerprint shown), a changed one shows old and new and can be replaced; no Google backup or device transfer of app data; `/tmp` uploads and editor saves never follow a planted link, and uploads are `0600`; release builds sign with `android/key.properties`; the private-key field stays out of keyboard learning | security audit codebase | 00f3aaa | UAT passed (backup off, checked over adb; new-host-key prompt, private-key field, editor save and http links passed by the user; uploads land as `-rw-------`, checked on DESKTOP-L2EPDPG; the planted-link trap was never set up, because the coordinator's setup was blocked for deleting the uploaded copy, and the user accepted that part; release signing can't be checked on a debug build) |
| Proxy jump: a host can connect through another saved host ("Jump host" in its edit page, like `ssh -J`; a jump host may have its own) | proxy jump settings | b8484f5 | UAT passed |
| Tailnet forwarding: never forward debugger ports (9229 etc.), stop a forward as soon as its server stops, reuse the same tailnet port when it comes back (restarting `bun dev` stacked 3002–3005 and exposed the inspector) | Coordinator | 5ed4f74 | UAT passed |
| Settings page (⚙ on the host list) with a terminal font picker (Cascadia Mono, Cascadia Code, CaskaydiaCove Nerd Font, JetBrains Mono, Fira Code) and font size, live everywhere | Coordinator | 09ced89 | UAT passed |
| App renamed to Clode (visible name only; package id, settings keys, tmux names and the sshbox:// scheme keep "sshbox") | Coordinator | 66d22e3 | UAT passed |
| The Tailscale sign-in link opens in a web tab too (Google may refuse embedded sign-in; Open in browser is the fallback) | Coordinator | bf149c7 | UAT passed |
| Tucked magic key: a short pull (~32 dp past the dead zone) switches the aim to ring 2, sliding back returns to ring 1 | Coordinator | b5d129c, 1da91aa | UAT passed |
| Hidden tabs cannot take keyboard focus; switching tabs focuses the page shown (keys typed after a file tab opens no longer reach the hidden shell) | Coordinator | b34a998, 6e112ce | UAT passed |
| Toasts in the toastification style at the top, auto-closing (1s by default); the port-forward notice becomes a 5s toast with Open | Coordinator | 6764896, 89f37e0 | UAT passed (after a retest: the first port-forward test ran a server started before the session connected) |
| Code editor: find and replace, go to line, and a search result opens at its line | text editor enhancement | bf404f2 | UAT passed |
| Code editor key bar: arrows and a cursor pad, Tab, undo/redo, symbols (the user found no way to move the cursor in a file) | text editor enhancement | 13c7831, 47d9013 | UAT passed (after a first failed try: physical-keyboard arrows did not move the editor cursor) |
| Holding a selection handle at the top or bottom edge keeps the terminal scrolling | Coordinator | 40bcdd9, 7df6997, 479c2a5, 46e9fcd | Dropped by the user after three failed UATs; removed in 46e9fcd |
| Links open as a web tab beside their shell (WebView); the Tailscale sign-in stays on a Custom Tab | Coordinator | de465b5, f54e12f, 0168e48 | UAT passed (after a first failed try where the Custom Tab opened a separate screen) |
| Native tmux split panes ("Use tmux" per host) | Coordinator | 89845de, eea0e47, f2c3d92 | UAT passed |
| Terminal selection with the platform's start/end handles and toolbar | Coordinator | dbd8375 | UAT passed |
| Follow's busy-check messages as a toast | Coordinator | b984c8e | UAT passed |
| Files drawer reopens at its last scroll position | Coordinator | 039ca21, 5dc262e | UAT passed |
| Long press on text selects it; on blank space it arms the arrows | Coordinator | ce6223c, dbd8375 | UAT passed (after a first failed try with no start/end handles) |
| Ctrl+tap a path or link to open it | Coordinator | dd981a7 | UAT passed. Reported broken on the tablet on 2026-09-21 and **it was a false alarm** — the user checked again and it works ("ternyata setelah aku cek lagi sudah bekerja"). Nothing was wrong and nothing was changed. Recorded because the episode cost real time and left two things worth keeping. First, the report reached the coordinator through the e2e session rather than from the user, and was passed on as proven when it was only reported; the coordinator did not attach the tablet on a peer's word, which is why the user's SSH sessions were never closed for a bug that did not exist. Second, and true either way: ctrl_click_test.dart is six pure tests over findLinks(), a string helper, with no testWidgets in the file — nothing arms CTRL, underlines, taps or opens, so the whole wiring could be deleted and the suite would stay green. That file cannot see this feature break, which is also why reading the source settled nothing. A regression test over the wiring is still wanted. The e2e session's own coverage audit, prompted by this, found 14 more entries marked as guarded by widget tests whose tests never build a widget, five of them real UI with nothing testing it, including two host-key surfaces. |
| Follow in terminal: every folder tapped in the tree, and never while a program runs | Coordinator | 47c3301, 3403407 | UAT passed |
| Code editor: line numbers, syntax colour, whole-file saves, drafts, sudo open/save | text editor enhancement | e20b9fe, 6bd0c2a, b418d69 | UAT passed |
| Long-press a shell tab → Duplicate session | Coordinator | 24c826a | UAT passed |
| File tab reads `<hostname> · file` | Coordinator | e2b66eb, a168f3a | UAT passed (after a first failed try that showed the profile label) |
| Magic key: two concentric rings, and it fades to 60% while idle | Coordinator | e3e7387, 30a4171, 29259ab | UAT passed |
| Magic key: throw it at a side to tuck it away | Coordinator | 7156040 | UAT passed |
| Terminal arrows only after a long press; a plain drag scrolls | Coordinator | 50e2107 | UAT passed |
| Shift+Enter on a hardware keyboard starts a new line | Coordinator | 02ef3c2 | UAT passed |
| Terminal page without its header: files and upload in the key bar, reconnect on the tab | Coordinator | 853997f | UAT passed |
| Forward servers started in a session to the tailnet | tailscale magic dns integration | 43e234d, fa53e25 | UAT passed |
| Files drawer is a VS Code-style tree, with a root saved in the host config | file tree redesign ssh root | 640461b, 8fe5a96 | UAT passed |
| Several sessions per host; the host list counts the active ones | config multi-session display | 7c84d2b | UAT passed |
| "Open in terminal" from the files drawer | file drawer open directory button | 30fda9b | UAT passed |
